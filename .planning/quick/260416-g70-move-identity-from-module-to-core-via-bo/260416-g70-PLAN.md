---
phase: 260416-g70
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BOOTSTRAP.md
  - bot/claude_query.py
  - bot/bridge/telegram.py
  - bot/modules_runtime/identity.py
  - bot/templates/modules/identity.md
  - modules/identity/manifest.json
  - modules/identity/install.sh
  - modules/identity/uninstall.sh
  - modules/identity/prompt.md
  - modules/identity/README.md
  - tests/modules/test_identity.py
  - tests/test_claude_query_bootstrap.py
  - scripts/reset-dev.sh
autonomous: true
requirements:
  - QT-260416-g70
must_haves:
  truths:
    - "BOOTSTRAP.md exists at repo root in the tracked working tree"
    - "When BOOTSTRAP.md exists, its content is injected into the Claude Code system prompt"
    - "When BOOTSTRAP.md is absent, no bootstrap content appears in the system prompt"
    - "Claude deleting BOOTSTRAP.md (via Write/Bash) stops injection on subsequent queries"
    - "No code path references modules/identity/ or build_onboarding_handler"
    - "/identity Telegram command is unregistered"
    - "scripts/reset-dev.sh is committed to the repo and restores BOOTSTRAP.md + wipes ~/hub/knowledge/identity/"
    - "pytest green after refactor; obsolete identity tests removed; new BOOTSTRAP.md injection test passes present-vs-absent"
  artifacts:
    - path: "BOOTSTRAP.md"
      provides: "Onboarding instructions injected when present; deleted by Claude when onboarding complete"
      min_lines: 20
    - path: "bot/claude_query.py"
      provides: "build_options injects BOOTSTRAP.md content (XML-wrapped) when the file exists at repo root"
      contains: "bootstrap"
    - path: "bot/bridge/telegram.py"
      provides: "No import of bot.modules_runtime.identity; no build_onboarding_handler registration"
    - path: "scripts/reset-dev.sh"
      provides: "Dev reset that restores BOOTSTRAP.md from git index and wipes identity hub dir"
      contains: "git checkout"
    - path: "tests/test_claude_query_bootstrap.py"
      provides: "Test: injection present when BOOTSTRAP.md exists, absent when it doesn't"
  key_links:
    - from: "bot/claude_query.py::build_options"
      to: "<repo_root>/BOOTSTRAP.md"
      via: "Path(__file__).resolve().parent.parent / 'BOOTSTRAP.md'"
      pattern: "BOOTSTRAP"
    - from: "bot/bridge/telegram.py::build_app"
      to: "(nothing — handler removed)"
      via: "removed: app.add_handler(build_onboarding_handler())"
      pattern: "NOT build_onboarding_handler"
    - from: "scripts/reset-dev.sh"
      to: "BOOTSTRAP.md + ~/hub/knowledge/identity/"
      via: "git checkout -- BOOTSTRAP.md && rm -rf ~/hub/knowledge/identity"
      pattern: "git checkout.*BOOTSTRAP"
---

<objective>
Replace the `modules/identity/` module + `ConversationHandler` Q1/Q2/Q3 onboarding
with a core BOOTSTRAP.md lifecycle: when `BOOTSTRAP.md` exists at repo root, its
contents are injected into the Claude Code system prompt; Claude is instructed
to mirror the user's language, conversationally learn about them, write
`~/hub/knowledge/identity/{USER,SOUL}.md`, then delete BOOTSTRAP.md to end the
onboarding state.

Purpose: onboarding becomes a single-file, operator-edited prompt — no more
hardcoded question script, no module install ceremony. Any future change to
onboarding behavior is an edit to BOOTSTRAP.md, not a code change.

Output:
- New `BOOTSTRAP.md` at repo root
- `bot/claude_query.py` injection hook
- `bot/bridge/telegram.py` cleaned of the handler
- `bot/modules_runtime/identity.py` and `modules/identity/` deleted
- `bot/templates/modules/identity.md` deleted
- Obsolete identity tests deleted + new injection test
- `scripts/reset-dev.sh` committed to repo with updated wipe steps
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260416-g70-move-identity-from-module-to-core-via-bo/260416-g70-CONTEXT.md
@.planning/STATE.md
@CLAUDE.md
@bot/claude_query.py
@bot/bridge/telegram.py
@bot/modules_runtime/identity.py
@bot/modules/assembler.py
@bot/templates/modules/identity.md
@modules/identity/manifest.json
@modules/identity/install.sh
@modules/identity/uninstall.sh
@modules/identity/prompt.md
@modules/identity/README.md
@tests/modules/test_identity.py

<interfaces>
<!-- Key contracts for executors. Do NOT re-explore. -->

From bot/claude_query.py (current state):
```python
HUB_KNOWLEDGE: Path = Path.home() / "hub" / "knowledge"
_PLACEHOLDER_MARKER = "<!-- animaya:placeholder -->"
_MAX_INJECT_CHARS = 8_000

def _read_for_injection(p: Path) -> str:
    """Returns file content (truncated, tag-escaped) or '' if missing/placeholder."""
    # ... escapes closing tags for ("identity-user", "identity-soul", "memory-core")

def build_options(data_dir=None, system_prompt_extra="", cwd=None):
    # assembles `parts: list[str]` then joins with "\n\n"
    # appends <identity-user>, <identity-soul>, <memory-core> blocks conditionally
    # returns ClaudeCodeOptions(system_prompt=..., allowed_tools=[Read,Write,Edit,Bash,...])
```

Repo root: three levels up from `bot/claude_query.py`, i.e.
`Path(__file__).resolve().parent.parent` (one `parent` → `bot/`, two → repo root).
Confirm by the pattern already in `bot/modules/assembler.py`:
```python
_REPO_ROOT = Path(__file__).resolve().parent.parent.parent  # from bot/modules/
```
So from `bot/claude_query.py`: `Path(__file__).resolve().parent.parent`.

From bot/bridge/telegram.py (lines to remove):
```python
# Line 32:
from bot.modules_runtime.identity import build_onboarding_handler
# Lines ~450-454 (comment block about IDEN-01 onboarding routing)
# Line 743-746 (comment + app.add_handler(build_onboarding_handler()))
```

From bot/modules_runtime/identity.py — entire file is deletion target.
Nothing else in bot/ currently imports from it (verified via grep: only
bot/bridge/telegram.py imports `build_onboarding_handler`).

From bot/modules/assembler.py:
The assembler reads from `registry.json` — identity module simply won't be in
the registry after uninstall. No assembler code change needed; `bot/templates/modules/identity.md`
is the installed-copy template (separate from `modules/identity/prompt.md`) and
should be deleted for cleanliness.

From tests/modules/test_identity.py (59 lines):
Tests `is_identity_initialized`, `mark_pending_onboarding`, `write_identity_files`,
`build_onboarding_handler` — all obsolete. DELETE the whole file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create BOOTSTRAP.md + wire system-prompt injection (TDD)</name>
  <files>
    BOOTSTRAP.md,
    bot/claude_query.py,
    tests/test_claude_query_bootstrap.py
  </files>
  <behavior>
    Test 1 (injection present): When `BOOTSTRAP.md` exists at repo root
    containing a known sentinel string ("BOOTSTRAP_TEST_MARKER"),
    `build_options(...)` returns `ClaudeCodeOptions` whose `system_prompt`
    contains that sentinel wrapped in a `<bootstrap>...</bootstrap>` block.

    Test 2 (injection absent): When `BOOTSTRAP.md` does not exist (or is
    empty), `build_options(...).system_prompt` contains no `<bootstrap>` tag
    and no BOOTSTRAP content.

    Test strategy: the existing `_read_for_injection` truncates/validates;
    add a new helper `_read_bootstrap_for_injection()` that reads from a
    REPO_ROOT path (derived in the module), OR parametrize `_read_for_injection`
    by path. For testability, expose the repo-root path as a module-level
    constant `REPO_ROOT = Path(__file__).resolve().parent.parent` and monkeypatch
    it in tests to point at a tmp directory. Tests create/omit
    `tmp_path / "BOOTSTRAP.md"` and assert on `build_options(...).system_prompt`.

    Edge case: do NOT treat BOOTSTRAP.md as a "placeholder" file — its whole
    point is to inject. But DO apply `_MAX_INJECT_CHARS` truncation and tag
    escaping for `</bootstrap>` to prevent prompt-injection escapes.
  </behavior>
  <action>
    1. **Create `BOOTSTRAP.md`** at repo root (`/Users/admin/hub/workspace/animaya/BOOTSTRAP.md`).
       Content: short (50-120 lines), operator-voiced. Must teach Claude:
       - **Language mirror rule:** reply in whatever language the operator's
         first message is written in (Russian/English/etc.) and keep using that
         language throughout onboarding.
       - **Natural elicitation:** no rigid Q1/Q2/Q3 script. Get to know the
         operator through normal conversation. Ask about who they are, what
         they do, what matters to them — but react to what they say, don't
         interrogate.
       - **Capture both USER and SOUL:** who they are (USER.md) AND how they
         want the assistant to behave toward them (SOUL.md — tone, priorities,
         form of address).
       - **Commit rule:** trust your own judgement on "enough". Err toward
         capturing less but correct, over more but invented. When ready,
         Write `~/hub/knowledge/identity/USER.md` and
         `~/hub/knowledge/identity/SOUL.md`, then delete this BOOTSTRAP.md
         file (Bash `rm BOOTSTRAP.md` from the repo root — note the working
         directory is the per-session cwd; use the absolute repo path).
       - **Absolute repo path:** BOOTSTRAP.md lives at the Animaya repo
         checkout on this bot. Include a hint: the bot's session cwd is
         `/data/sessions/...`, NOT the repo; Claude must use an absolute path
         when deleting (e.g., locate via `git rev-parse --show-toplevel` from
         the repo, or use the known LXC path `/home/animaya/animaya/BOOTSTRAP.md`
         — prefer a rg/find to locate by filename to be robust).
       - Tone: operator instructions, NOT a script the user sees.

    2. **Write the test file FIRST** (`tests/test_claude_query_bootstrap.py`)
       describing Test 1 + Test 2 above. Use pytest's `monkeypatch` to set
       `bot.claude_query.REPO_ROOT = tmp_path`. Run to confirm it fails
       (there's no REPO_ROOT or bootstrap injection yet). This is the RED step.

    3. **Implement** in `bot/claude_query.py`:
       - Add module-level `REPO_ROOT: Path = Path(__file__).resolve().parent.parent`
         (one `.parent` from `bot/claude_query.py` → `bot/`; two → repo root).
       - Add a helper (or extend `_read_for_injection`) to read BOOTSTRAP.md.
         Respect `_MAX_INJECT_CHARS` truncation. Escape `</bootstrap>` →
         `&lt;/bootstrap&gt;`. Do NOT apply the placeholder-marker skip (the
         file is small, human-written, always intentional).
       - In `build_options(...)`, append a `<bootstrap>\n{content}\n</bootstrap>`
         block to `parts` BEFORE the identity/memory blocks so Claude sees
         onboarding instructions first. Only append when non-empty.
       - Update the existing `_read_for_injection` tag-escape tuple to also
         include `"bootstrap"` if you route bootstrap through it.

    4. Run the test — must pass (GREEN).
       `python -m pytest tests/test_claude_query_bootstrap.py -v`

    5. **Do NOT modify other build_options callers** — the signature is
       unchanged.
  </action>
  <verify>
    <automated>python -m pytest tests/test_claude_query_bootstrap.py -v</automated>
  </verify>
  <done>
    - `BOOTSTRAP.md` exists at repo root with operator-voiced onboarding guidance (language mirror + natural elicitation + when-to-commit + delete-self rule).
    - `bot/claude_query.py` injects `<bootstrap>...</bootstrap>` into `system_prompt` iff BOOTSTRAP.md exists.
    - Two-case test (present + absent) passes.
    - `REPO_ROOT` is a module-level constant so tests can monkeypatch it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rip out identity module code, handlers, templates, and obsolete tests</name>
  <files>
    bot/bridge/telegram.py,
    bot/modules_runtime/identity.py,
    bot/templates/modules/identity.md,
    modules/identity/manifest.json,
    modules/identity/install.sh,
    modules/identity/uninstall.sh,
    modules/identity/prompt.md,
    modules/identity/README.md,
    tests/modules/test_identity.py
  </files>
  <action>
    Ordered removal — do these in sequence so intermediate states remain importable:

    1. **Edit `bot/bridge/telegram.py`:**
       - Delete line 32: `from bot.modules_runtime.identity import build_onboarding_handler`.
       - Delete the IDEN-01 comment block in `_handle_message` (lines ~450-454,
         starting "# IDEN-01 onboarding routing is owned by build_onboarding_handler() —").
       - Delete the 3-line comment + the registration call near line 743-746:
         ```
         # IDEN-04: /identity reconfigure ConversationHandler MUST be registered
         # BEFORE the catch-all MessageHandler so /identity is routed correctly
         # and Q&A state messages stay inside the conversation (Pitfall 8).
         app.add_handler(build_onboarding_handler())
         ```
         Leave the surrounding handler registrations (`_claim_handler`,
         `_owner_gate`, `/start` CommandHandler, catch-all MessageHandler,
         error handler) untouched.

    2. **Delete `bot/modules_runtime/identity.py`** entirely. No shim — the
       assembler doesn't import it; only telegram.py did, and that import is
       gone from step 1. Verify no other importers:
       `grep -rn "modules_runtime.identity\|modules_runtime\.identity" bot/ tests/ || true`

    3. **Delete `bot/templates/modules/identity.md`** (7 lines; installed-copy
       template for the defunct module).

    4. **Delete `modules/identity/` directory and all its files**
       (manifest.json, install.sh, uninstall.sh, prompt.md, README.md).
       Use `rm -rf modules/identity/`. Preserve the `modules/` parent directory
       (other modules live there — don't touch them).

    5. **Delete `tests/modules/test_identity.py`** (59 lines, all tests
       target the deleted module). The `tests/modules/` directory stays;
       verify it still has other tests or at least a conftest.

    6. **Run the full test suite** to confirm no regression:
       `python -m pytest tests/ -x -q`.
       Expected: green. If any test fails due to a stray `modules/identity`
       reference elsewhere in tests/, fix it (most likely removal of imports
       in `tests/modules/conftest.py` or `tests/test_install.py`). Do not
       preserve assertions about the identity module existing — it's gone.

    7. **Ruff lint:** `ruff check bot/ tests/` — must be clean.

    Nothing else to wire. The assembler (`bot/modules/assembler.py`) reads
    `registry.json` at runtime; after operators run `reset-dev.sh` or simply
    remove the identity entry, the module drops out of CLAUDE.md assembly
    naturally. No assembler code change required (per CONTEXT.md Discretion).
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q &amp;&amp; ruff check bot/ tests/ &amp;&amp; ! grep -rn "build_onboarding_handler\|modules_runtime\.identity\|modules/identity" bot/ tests/</automated>
  </verify>
  <done>
    - `bot/modules_runtime/identity.py` no longer exists.
    - `bot/templates/modules/identity.md` no longer exists.
    - `modules/identity/` directory no longer exists.
    - `tests/modules/test_identity.py` no longer exists.
    - `bot/bridge/telegram.py` has zero references to `build_onboarding_handler`, `modules_runtime.identity`, or `/identity`.
    - `pytest tests/` green.
    - `ruff check` green.
    - grep for `build_onboarding_handler|modules_runtime\.identity|modules/identity` inside `bot/` and `tests/` returns empty.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add scripts/reset-dev.sh to repo with BOOTSTRAP.md restore + identity wipe</name>
  <files>scripts/reset-dev.sh</files>
  <action>
    Create `scripts/reset-dev.sh` (NEW FILE in repo — it currently exists on
    the LXC at `/home/animaya/animaya/scripts/reset-dev.sh` but is NOT tracked
    in git). This task commits the authoritative version.

    The script MUST run under bash, use `set -euo pipefail`, and do:

    1. **Cd to repo root** via `cd "$(git rev-parse --show-toplevel)"`.
       If `git rev-parse` fails (not in a repo), fall back to `cd "$(dirname "$0")/.."`.

    2. **Restore BOOTSTRAP.md from the index** (tracked file, restored to its
       committed version): `git checkout -- BOOTSTRAP.md`.
       If BOOTSTRAP.md is untracked (first-time setup), warn and skip.

    3. **Wipe the identity hub directory:**
       `rm -rf "$HOME/hub/knowledge/identity"` (the directory, since
       BOOTSTRAP.md instructs Claude to write USER.md/SOUL.md there).
       Safe on missing dir (rm -rf is idempotent).

    4. **Preserve whatever existing wipe logic the LXC version already has.**
       Since the repo version doesn't exist yet, base this on the minimum viable
       reset flow: additional wipes we know about from prior phases are
       acceptable (e.g., claim state in `/data/modules/telegram-bridge/state.json`,
       session dirs under `/data/sessions/`). BUT — CONTEXT.md only asserts
       BOOTSTRAP.md + identity dir, so stick to those two operations and leave
       a commented-out section "# Add more wipes here as needed" to avoid
       overreaching. If the operator has a richer version on the LXC they'll
       merge manually — the repo version is the minimum viable reset for the
       BOOTSTRAP.md lifecycle this task introduces.

    5. **Log each step** to stdout with `echo "[reset-dev] ..."` prefixes so
       the operator sees what happened.

    6. **Exit 0 on success.**

    7. **`chmod +x scripts/reset-dev.sh`** so it's executable when checked out.

    Reference shape (adjust wording/ordering; this is guidance, not a spec):

    ```bash
    #!/usr/bin/env bash
    # Reset the dev bot's transient state: restore BOOTSTRAP.md and wipe
    # Claude-generated identity files so onboarding runs again from scratch.
    set -euo pipefail

    repo_root="$(git rev-parse --show-toplevel 2>/dev/null || echo "")"
    if [[ -z "$repo_root" ]]; then
      repo_root="$(cd "$(dirname "$0")/.." && pwd)"
    fi
    cd "$repo_root"

    echo "[reset-dev] repo: $repo_root"

    if git ls-files --error-unmatch BOOTSTRAP.md >/dev/null 2>&1; then
      echo "[reset-dev] restoring BOOTSTRAP.md from index"
      git checkout -- BOOTSTRAP.md
    else
      echo "[reset-dev] BOOTSTRAP.md not tracked yet — skipping restore"
    fi

    identity_dir="$HOME/hub/knowledge/identity"
    if [[ -d "$identity_dir" ]]; then
      echo "[reset-dev] wiping $identity_dir"
      rm -rf "$identity_dir"
    else
      echo "[reset-dev] $identity_dir not present — nothing to wipe"
    fi

    # Add more wipes here as needed (claim state, sessions, etc.)

    echo "[reset-dev] done"
    ```

    Do NOT add tests for this script in this task — the automated verify below
    smoke-tests it by running it in a tmp dir; no pytest file needed.
  </action>
  <verify>
    <automated>test -x scripts/reset-dev.sh &amp;&amp; bash -n scripts/reset-dev.sh &amp;&amp; grep -q "git checkout -- BOOTSTRAP.md" scripts/reset-dev.sh &amp;&amp; grep -q "rm -rf" scripts/reset-dev.sh &amp;&amp; grep -q "hub/knowledge/identity" scripts/reset-dev.sh</automated>
  </verify>
  <done>
    - `scripts/reset-dev.sh` exists in the repo, is executable, and passes `bash -n` syntax check.
    - Script runs `git checkout -- BOOTSTRAP.md` and `rm -rf ~/hub/knowledge/identity`.
    - Script is idempotent (handles missing files gracefully).
    - File is staged for the final commit of this quick task.
  </done>
</task>

</tasks>

<verification>
End-to-end phase check after all three tasks:

1. `python -m pytest tests/ -v` — all green, including the new
   `tests/test_claude_query_bootstrap.py`, and without the deleted
   `tests/modules/test_identity.py`.
2. `ruff check bot/ tests/` — clean.
3. Grep audit:
   - `grep -rn "build_onboarding_handler\|modules_runtime\.identity" bot/ tests/` → empty
   - `grep -rn "modules/identity" bot/ tests/` → empty
   - `ls modules/identity 2>&1` → "No such file or directory"
   - `ls bot/modules_runtime/identity.py 2>&1` → "No such file or directory"
4. Manual boot smoke (optional, not blocking): `python -c "from bot.bridge.telegram import build_app; print('ok')"` — imports cleanly.
5. BOOTSTRAP.md exists at repo root and is tracked (`git ls-files BOOTSTRAP.md` returns it).
6. `scripts/reset-dev.sh` is executable and tracked.
</verification>

<success_criteria>
- BOOTSTRAP.md lifecycle is the ONLY onboarding path in the codebase.
- System prompt carries `<bootstrap>...</bootstrap>` iff BOOTSTRAP.md exists at repo root.
- No `/identity` command, no ConversationHandler, no `.pending-onboarding` sentinel, no `modules/identity/` dir anywhere in bot/, tests/, or modules/.
- `scripts/reset-dev.sh` is committed and restores BOOTSTRAP.md + wipes identity dir in one command.
- Existing already-onboarded bots keep working (identity injection from USER.md/SOUL.md is unchanged — we only ADDED the bootstrap block).
- Operator workflow for re-onboarding: run `scripts/reset-dev.sh`; next Telegram message triggers fresh bootstrap flow; Claude writes USER.md/SOUL.md and deletes BOOTSTRAP.md.
</success_criteria>

<output>
After completion, create `.planning/quick/260416-g70-move-identity-from-module-to-core-via-bo/260416-g70-SUMMARY.md` documenting:
- Files created / modified / deleted (counts + paths)
- Test results (new test added, obsolete tests removed)
- Any deviations from the plan (e.g., if the assembler DID need updating after all)
- The exact BOOTSTRAP.md wording chosen (since CONTEXT.md left it to Claude's discretion)
- Operator reset command: `bash scripts/reset-dev.sh`
</output>
