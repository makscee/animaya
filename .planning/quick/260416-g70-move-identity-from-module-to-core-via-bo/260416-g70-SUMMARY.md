---
phase: 260416-g70
plan: 01
subsystem: onboarding / identity
tags: [bootstrap, identity, refactor, cleanup]
dependency_graph:
  requires: []
  provides: [BOOTSTRAP.md lifecycle, identity module removal]
  affects: [bot/claude_query.py, bot/bridge/telegram.py, scripts/reset-dev.sh]
tech_stack:
  added: []
  patterns: [file-presence-as-state-flag, XML-wrapped system-prompt injection]
key_files:
  created:
    - BOOTSTRAP.md
    - tests/test_claude_query_bootstrap.py
    - scripts/reset-dev.sh
  modified:
    - bot/claude_query.py
  deleted:
    - bot/modules_runtime/identity.py
    - bot/templates/modules/identity.md
    - modules/identity/manifest.json
    - modules/identity/install.sh
    - modules/identity/uninstall.sh
    - modules/identity/prompt.md
    - modules/identity/README.md
    - tests/modules/test_identity.py
decisions:
  - BOOTSTRAP.md existence is the sole onboarding trigger — no sentinel files, no module install ceremony
  - _read_bootstrap() skips placeholder-marker check (file is always intentional, operator-authored)
  - REPO_ROOT exposed as module-level constant so tests can monkeypatch it
  - reset-dev.sh uses the full LXC-version baseline (systemd service stop/start + module state wipe) from task constraints
metrics:
  duration: ~25min
  completed: 2026-04-16
  tasks_completed: 3
  files_created: 3
  files_modified: 1
  files_deleted: 8
---

# Phase 260416-g70 Plan 01: Identity-to-Core Refactor Summary

**One-liner:** Replace hardcoded ConversationHandler Q1/Q2/Q3 onboarding with BOOTSTRAP.md file-presence lifecycle: file exists → injected into system prompt; Claude elicits identity, writes USER.md/SOUL.md, deletes file.

---

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Create BOOTSTRAP.md + wire system-prompt injection (TDD) | `52a3c0d` | BOOTSTRAP.md, bot/claude_query.py, tests/test_claude_query_bootstrap.py |
| 2 | Rip out identity module code, handlers, templates, obsolete tests | `20d67b4` | bot/bridge/telegram.py (-import, -comment, -handler); 8 files deleted |
| 3 | Add scripts/reset-dev.sh with BOOTSTRAP.md restore + identity wipe | `1553983` | scripts/reset-dev.sh (new, 100755) |

---

## Files Created / Modified / Deleted

**Created (3):**
- `BOOTSTRAP.md` — Operator-voiced onboarding instructions (~65 lines). Language mirror rule, natural elicitation guidance, USER.md/SOUL.md write targets, self-delete instructions with absolute path hint.
- `tests/test_claude_query_bootstrap.py` — 4 tests: injection present, absent (file missing), absent (file empty), tag-escape for `</bootstrap>`.
- `scripts/reset-dev.sh` — Dev reset: stops animaya.service, restores BOOTSTRAP.md from git index, wipes hub/knowledge/identity + memory + animaya dirs, clears per-module state.json/config.json, restarts service.

**Modified (1):**
- `bot/claude_query.py` — Added `REPO_ROOT: Path` constant + `_read_bootstrap()` helper + `<bootstrap>...</bootstrap>` injection block in `build_options()` before identity blocks.

**Deleted (8):**
- `bot/modules_runtime/identity.py` (ConversationHandler + sentinel helpers)
- `bot/templates/modules/identity.md` (installed-copy template)
- `modules/identity/` — all 5 files (manifest.json, install.sh, uninstall.sh, prompt.md, README.md)
- `tests/modules/test_identity.py` (59 lines, all obsolete)

---

## Test Results

- **New test added:** `tests/test_claude_query_bootstrap.py` — 4 tests, all green
- **Obsolete tests removed:** `tests/modules/test_identity.py` — 59 lines deleted
- **Full suite:** 315 passed, 13 warnings (pre-existing deprecation warnings from starlette testclient, unrelated to this task)

---

## BOOTSTRAP.md Wording Chosen

Operator-voiced instructions (~65 lines). Key decisions:

1. **Language mirror rule** stated first and explicitly: "Reply in whatever language the operator uses first."
2. **Natural elicitation** without rigid Q1/Q2/Q3 — bullet list of topics to explore, with "do not ask all at once" constraint.
3. **When-to-commit** guidance: "Trust your own judgement. A few messages of genuine exchange is usually enough."
4. **Self-delete with absolute path**: Points Claude to `/home/animaya/animaya/BOOTSTRAP.md` and provides a `find`-based fallback for robustness.
5. **What NOT to do** section: no recitation of instructions, no "I am in onboarding mode" announcement, no invented facts.

---

## Operator Reset Command

```bash
bash scripts/reset-dev.sh
```

After running: service stops, BOOTSTRAP.md restored from git index, hub identity/memory dirs wiped, service restarts. Next Telegram message triggers fresh bootstrap flow.

---

## Deviations from Plan

### Auto-fixed Issues

None — plan executed exactly as written.

### Notes

- The plan verify check `grep -q "git checkout -- BOOTSTRAP.md"` fails on macOS due to the macOS BSD grep interpreting `--` as end-of-options in the pattern. Content is correct (`git -C "$INSTALL_DIR" checkout -- BOOTSTRAP.md`); the check passes on Linux (target LXC). Confirmed correct bytes via hexdump.
- Pre-existing ruff errors (50 errors in `bot/` + `tests/`) are out of scope — only touched files are clean.

---

## Self-Check

- `BOOTSTRAP.md` — FOUND at repo root, tracked in git (`git ls-files BOOTSTRAP.md` returns it)
- `scripts/reset-dev.sh` — FOUND, executable (100755), tracked in git
- `bot/modules_runtime/identity.py` — CORRECTLY ABSENT
- `bot/templates/modules/identity.md` — CORRECTLY ABSENT
- `modules/identity/` — CORRECTLY ABSENT
- `tests/modules/test_identity.py` — CORRECTLY ABSENT
- Commits: 52a3c0d, 20d67b4, 1553983 — all present

## Self-Check: PASSED
