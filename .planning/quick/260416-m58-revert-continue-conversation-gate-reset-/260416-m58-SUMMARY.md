---
plan_id: 260416-m58-01
name: revert continue_conversation gate + reset-dev wipes ~/.claude/projects
mode: quick
tasks_completed: 2
tasks_total: 2
duration_minutes: ~5
completed_date: 2026-04-16
commits:
  - hash: 3e15cee
    message: "fix(quick-260416-m58): revert continue_conversation gate — bootstrap preserves session context"
    files: [bot/claude_query.py, tests/test_claude_query_bootstrap.py]
  - hash: b5b413e
    message: "chore(quick-260416-m58): reset-dev wipes ~/.claude/projects for fresh SDK session"
    files: [scripts/reset-dev.sh]
---

# Quick Task 260416-m58 Summary

## One-liner

Reverted `continue_conversation=not in_bootstrap_mode` gate back to `True`; reset-dev.sh now wipes `~/.claude/projects/` for a genuinely fresh SDK session on each operator reset.

## What Changed

### Task 1 — bot/claude_query.py + tests

- `build_options()`: removed `in_bootstrap_mode` local variable and the INFO log line added by 260416-l1z
- `continue_conversation` now unconditionally `True` — bootstrap injection (content, locale substitution) still functions as before
- `tests/test_claude_query_bootstrap.py`: deleted 3 tests that asserted `continue_conversation=False` when BOOTSTRAP.md present (`test_continue_conversation_false_when_bootstrap_present`, `test_continue_conversation_true_when_bootstrap_absent`, `test_continue_conversation_true_when_bootstrap_empty`) and the `_options()` helper; 10 remaining tests (injection, tag escape, locale substitution) all green

### Task 2 — scripts/reset-dev.sh

- Added `rm -rf "$HOME/.claude/projects"` with echo after `systemctl --user stop` and before `git checkout -- BOOTSTRAP.md`
- Ensures SDK transcript is wiped on operator reset, so onboarding always starts with no prior conversation history

## Test Evidence

```
10 passed in 0.52s  (tests/test_claude_query_bootstrap.py)
ruff: All checks passed
```

## Deviations

None — plan executed exactly as written.
