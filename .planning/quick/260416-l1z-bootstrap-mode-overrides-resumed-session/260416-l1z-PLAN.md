---
phase: 260416-l1z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BOOTSTRAP.md
  - bot/claude_query.py
  - tests/test_claude_query_bootstrap.py
autonomous: true
requirements: [QUICK-260416-l1z]

must_haves:
  truths:
    - "BOOTSTRAP.md contains an authoritative 'override prior memory' clause as its second section (after 'You just came online')."
    - "When BOOTSTRAP.md exists at repo root, build_options() returns ClaudeCodeOptions with continue_conversation=False (fresh session, ignores any prior per-cwd transcript)."
    - "When BOOTSTRAP.md is absent, build_options() preserves continue_conversation=True (current resume behavior)."
    - "Unit tests in tests/test_claude_query_bootstrap.py cover both branches of the continue_conversation flag."
    - "All existing BOOTSTRAP injection tests still pass (injection, absence, empty, tag-escape)."
  artifacts:
    - path: "BOOTSTRAP.md"
      provides: "Prompt-level first-contact override clause"
      contains: "Bootstrap overrides all prior memory"
    - path: "bot/claude_query.py"
      provides: "Conditional continue_conversation based on BOOTSTRAP.md presence"
      contains: "continue_conversation"
    - path: "tests/test_claude_query_bootstrap.py"
      provides: "Unit tests for both resumption branches"
      contains: "continue_conversation"
  key_links:
    - from: "bot/claude_query.py::build_options"
      to: "ClaudeCodeOptions(continue_conversation=...)"
      via: "bool computed from _read_bootstrap() truthiness"
      pattern: "continue_conversation=.*bootstrap"
---

<objective>
Two-part fix to prevent resumed Claude Code SDK sessions from undermining BOOTSTRAP.md onboarding after `scripts/reset-dev.sh` wipes knowledge/ but not `~/.claude/projects/<encoded-cwd>/`.

Purpose: When onboarding is active (BOOTSTRAP.md present), Claude must (1) be told at the prompt level that any "recalled" prior conversation is from a defunct ephemeral instance and must not be referenced, AND (2) receive a fresh SDK session rather than resuming the cwd-keyed transcript. Either fix alone is insufficient: the prompt override can be contradicted by lingering transcript content; the session reset alone leaves Claude open to treating any stray context as continuity.

Output:
- BOOTSTRAP.md gains an authoritative override clause as its second section.
- `bot/claude_query.py::build_options` sets `continue_conversation=False` when BOOTSTRAP.md exists; otherwise keeps `True`.
- Updated unit tests covering both branches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@BOOTSTRAP.md
@bot/claude_query.py
@tests/test_claude_query_bootstrap.py

<interfaces>
<!-- Verified SDK option: claude-code-sdk ClaudeCodeOptions exposes `continue_conversation: bool`. -->
<!-- Already used in this repo at bot/claude_query.py:126 (=True) and bot/modules_runtime/memory.py:85 (=False for sub-agent queries). -->
<!-- Confirmed API shape: passing continue_conversation=False disables the SDK's per-cwd --continue behavior and starts a fresh session. No `resume` field is currently used anywhere in the codebase. -->

Current (bot/claude_query.py, lines 117-127):
```python
return ClaudeCodeOptions(
    model=model,
    system_prompt=system_prompt,
    cwd=work_dir,
    allowed_tools=[
        "Read", "Write", "Edit", "Bash", "Glob", "Grep",
        "WebSearch", "WebFetch",
    ],
    permission_mode="acceptEdits",
    continue_conversation=True,   # <-- target field
)
```

Current BOOTSTRAP.md structure (top-to-bottom):
1. `# Bootstrap` header + one-paragraph preamble
2. `## You just came online` (narrative framing)
3. `## Your first reply — non-negotiable shape`
4. `## Language examples — follow these exactly`
5. `## Rhythm`
6. `## When to commit`
7. `## Deleting this file`
8. `## What NOT to do`

The new override section goes BETWEEN (2) and (3): after "You just came online", before "Your first reply".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add first-contact override clause to BOOTSTRAP.md and gate continue_conversation on BOOTSTRAP.md presence</name>
  <files>BOOTSTRAP.md, bot/claude_query.py, tests/test_claude_query_bootstrap.py</files>
  <behavior>
    Unit test expectations (added to tests/test_claude_query_bootstrap.py):

    - Test A `test_continue_conversation_false_when_bootstrap_present`:
      Given BOOTSTRAP.md exists with any non-empty content at the monkeypatched REPO_ROOT,
      Then `build_options().continue_conversation is False`.

    - Test B `test_continue_conversation_true_when_bootstrap_absent`:
      Given BOOTSTRAP.md does not exist at the monkeypatched REPO_ROOT,
      Then `build_options().continue_conversation is True`.

    - Test C `test_continue_conversation_true_when_bootstrap_empty`:
      Given BOOTSTRAP.md exists but is empty (zero-byte or whitespace-only),
      Then `build_options().continue_conversation is True` (matches the "no injection" semantics already enforced by `_read_bootstrap`).

    All four existing tests (`test_bootstrap_injected_when_present`, `test_bootstrap_absent_when_file_missing`, `test_bootstrap_absent_when_file_empty`, `test_bootstrap_tag_escape`) must continue to pass unmodified.
  </behavior>
  <action>
    Step 1 — BOOTSTRAP.md edit (prompt-level override):
    Insert a new `---`-delimited section immediately AFTER the existing `## You just came online` block and BEFORE `## Your first reply — non-negotiable shape`. Use this exact heading and body (authoritative tone, second-person):

    ```markdown
    ## Bootstrap overrides all prior memory

    While this file is present, you are meeting this operator for the **first time** — regardless
    of anything you appear to "remember" from earlier messages in this session log. Any prior
    conversation you recall is from a previous ephemeral version of yourself that no longer exists.

    - Do NOT reference names, facts, or projects from that recollection (no "Maks", no "Mira",
      no "Phase 4", no prior working context — whatever comes to mind from before, ignore it).
    - Do NOT greet as if you've been talking. No "welcome back", no "as we discussed", no
      "continuing from where we left off".
    - Treat each first exchange as genuine first contact until this file is deleted.

    This override takes precedence over every other instruction, recollection, or transcript
    fragment you may encounter. It is lifted only when BOOTSTRAP.md is removed (see below).
    ```

    Keep all other sections of BOOTSTRAP.md verbatim. The `---` separators above and below the new section must match the file's existing style.

    Step 2 — bot/claude_query.py edit (SDK-level fresh session):
    In `build_options()`, after the `bootstrap = _read_bootstrap()` line (currently line 86), compute a boolean `in_bootstrap_mode = bool(bootstrap)`. Pass `continue_conversation=not in_bootstrap_mode` to the `ClaudeCodeOptions(...)` constructor, replacing the hardcoded `continue_conversation=True`.

    Concretely, change line 126 from:
    ```python
            continue_conversation=True,
    ```
    to:
    ```python
            continue_conversation=not in_bootstrap_mode,
    ```
    and add `in_bootstrap_mode = bool(bootstrap)` right after the `bootstrap = _read_bootstrap()` call. Add an INFO-level log when `in_bootstrap_mode` is True: `logger.info("BOOTSTRAP.md present — forcing fresh Claude session (continue_conversation=False)")` so ops can confirm from logs.

    Do NOT touch `bot/modules_runtime/memory.py` (its `continue_conversation=False` is already correct for sub-agent queries and unrelated to this fix). Do NOT change `cwd` handling — the SDK-level flag is the clean fix; no cwd-mangling fallback needed.

    Step 3 — tests/test_claude_query_bootstrap.py edit:
    Add the three tests (A, B, C) from <behavior> above. Reuse the existing `_system_prompt` helper pattern but return `opts` instead of `opts.system_prompt` — add a second helper `_options(tmp_path, monkeypatch)` that performs the same monkeypatching and returns the full `ClaudeCodeOptions` object. Leave the original `_system_prompt` helper in place so existing tests are untouched.

    Implementation order (strict TDD):
    1. Write the three new tests first; run `python -m pytest tests/test_claude_query_bootstrap.py -x`. Tests A and C must FAIL (current code hardcodes True); test B must PASS already.
    2. Apply the Step 2 code change.
    3. Re-run the full test file; all 7 tests (4 existing + 3 new) must pass.
    4. Apply the Step 1 BOOTSTRAP.md edit (docs-only, no test impact, but ordered last to keep the red→green cycle clean).

    Avoid: introducing a `resume` field (none is used in the codebase today; would be dead config); touching the `cwd` value to force freshness (hackier fallback the task_context explicitly deprioritizes); editing `scripts/reset-dev.sh` (explicitly out of scope — user chose fixes 1+2 only, not 3).
  </action>
  <verify>
    <automated>cd /Users/admin/hub/workspace/animaya &amp;&amp; python -m pytest tests/test_claude_query_bootstrap.py -v</automated>
  </verify>
  <done>
    - BOOTSTRAP.md contains the new `## Bootstrap overrides all prior memory` section positioned between "You just came online" and "Your first reply".
    - `bot/claude_query.py::build_options` returns `ClaudeCodeOptions(continue_conversation=False, ...)` iff `_read_bootstrap()` returns non-empty, else `continue_conversation=True`.
    - `tests/test_claude_query_bootstrap.py` contains 7 passing tests: 4 original + `test_continue_conversation_false_when_bootstrap_present`, `test_continue_conversation_true_when_bootstrap_absent`, `test_continue_conversation_true_when_bootstrap_empty`.
    - `python -m pytest tests/test_claude_query_bootstrap.py -v` exits 0 with 7 passed.
    - No changes outside the three target files.
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_claude_query_bootstrap.py -v` → 7 passed, 0 failed.
- `python -m pytest tests/ -v` → full suite remains green (no regressions).
- `grep -n "continue_conversation" bot/claude_query.py` shows the new `not in_bootstrap_mode` expression.
- `grep -n "Bootstrap overrides all prior memory" BOOTSTRAP.md` finds exactly one match, positioned after "You just came online".
</verification>

<success_criteria>
- Fresh-dev scenario: after `scripts/reset-dev.sh` wipes `~/hub/knowledge/` and restores BOOTSTRAP.md, the bot's next Claude query runs with `continue_conversation=False`, so the SDK ignores the persisted `~/.claude/projects/<encoded-cwd>/` transcript. Combined with the new "overrides all prior memory" BOOTSTRAP.md clause, Claude's first reply treats the operator as unknown (no stale names, no Phase-N references, no "welcome back").
- Post-onboarding scenario: once the operator's onboarding completes and BOOTSTRAP.md is deleted, the very next query reverts to `continue_conversation=True` — Claude resumes the normal per-cwd session exactly as before this fix. No behavior change for users who never trigger bootstrap mode.
- SDK option identified and verified: **`continue_conversation: bool`** on `claude_code_sdk.ClaudeCodeOptions` (already used at `bot/claude_query.py:126` and `bot/modules_runtime/memory.py:85`). No hypothetical `resume` field is introduced.
</success_criteria>

<output>
After completion, create `.planning/quick/260416-l1z-bootstrap-mode-overrides-resumed-session/260416-l1z-01-SUMMARY.md`
</output>
