---
phase: 13
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/admin/hub/knowledge/references/ui-style-spec.md
  - /Users/admin/hub/knowledge/voidnet/ui-spec.md
  - dashboard/package.json
  - dashboard/tsconfig.json
  - dashboard/next.config.mjs
  - dashboard/postcss.config.mjs
  - dashboard/eslint.config.mjs
  - dashboard/bunfig.toml
  - dashboard/components.json
  - dashboard/test-setup.ts
  - dashboard/app/globals.css
  - dashboard/app/layout.tsx
  - dashboard/app/error.tsx
  - dashboard/app/not-found.tsx
  - dashboard/lib/utils.ts
  - dashboard/components/ui/
  - dashboard/playwright.config.ts
  - dashboard/tests/e2e/fixtures.ts
  - dashboard/.gitignore
autonomous: true
requirements: [D-02, D-11, D-12, D-13, D-14]
tags: [frontend, scaffolding, ui-spec]
must_haves:
  truths:
    - "Shared UI style spec exists at ~/hub/knowledge/references/ui-style-spec.md (filename Claude's discretion) and references homelab/apps/admin as its source of truth"
    - "~/hub/knowledge/voidnet/ui-spec.md is marked OBSOLETE at the top (pointer to new spec)"
    - "dashboard/ directory exists with package.json, tsconfig, eslint, postcss, bunfig, components.json, globals.css all spec-verbatim from homelab/apps/admin"
    - "bun install succeeds and produces bun.lockb; bun run build produces .next/standalone"
    - "All 19 shadcn primitives exist under dashboard/components/ui/*.tsx, copied verbatim from homelab/apps/admin"
    - "playwright.config.ts + fixtures exist; `bunx playwright test --list` succeeds"
  artifacts:
    - path: "/Users/admin/hub/knowledge/references/ui-style-spec.md"
      provides: "Shared UI style spec extracted from homelab/apps/admin"
      contains: "slate palette"
    - path: "dashboard/package.json"
      provides: "Spec §2 dep list with animaya scope + port 8090 -H 127.0.0.1"
      contains: "\"next\": \"15.5.15\""
    - path: "dashboard/app/globals.css"
      provides: "Tailwind v4 CSS-first theme (spec §4 verbatim)"
      contains: "@import \"tailwindcss\""
    - path: "dashboard/components/ui/button.tsx"
      provides: "shadcn Button (verbatim from admin)"
      contains: "cva"
    - path: "dashboard/playwright.config.ts"
      provides: "Playwright runner"
  key_links:
    - from: "dashboard/package.json"
      to: "frontend-stack-spec.md §2"
      via: "pinned versions"
      pattern: "\"next\":\\s*\"15\\.5\\.15\""
    - from: "dashboard/components.json"
      to: "dashboard/components/ui/*.tsx"
      via: "shadcn CLI alias"
      pattern: "\"ui\":"
---

<objective>
Wave 1 foundation: author shared UI style spec (D-12), scaffold the `dashboard/` Next.js project verbatim from `homelab/apps/admin` per spec, and stand up the test harness (Playwright + bun test).

Purpose: unblock every subsequent plan. No route logic yet; just the skeleton + tooling + spec artifact that downstream phases consume.
Output: authoritative `ui-style-spec.md` in hub, scaffolded `dashboard/`, 19 shadcn primitives, Playwright config, deprecation notice on obsolete voidnet/ui-spec.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-migrate-dashboard-frontend-to-shared-frontend-stack-spec/13-CONTEXT.md
@.planning/phases/13-migrate-dashboard-frontend-to-shared-frontend-stack-spec/13-RESEARCH.md
@.planning/phases/13-migrate-dashboard-frontend-to-shared-frontend-stack-spec/13-PATTERNS.md
@/Users/admin/hub/knowledge/references/frontend-stack-spec.md
@/Users/admin/hub/workspace/homelab/apps/admin/package.json
@/Users/admin/hub/workspace/homelab/apps/admin/app/globals.css
@/Users/admin/hub/workspace/homelab/apps/admin/eslint.config.mjs
@/Users/admin/hub/workspace/homelab/apps/admin/tsconfig.json
@/Users/admin/hub/workspace/homelab/apps/admin/components.json
@/Users/admin/hub/knowledge/voidnet/ui-spec.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author shared ui-style-spec.md and deprecate voidnet/ui-spec.md (D-12)</name>
  <read_first>
    - /Users/admin/hub/workspace/homelab/apps/admin/app/globals.css (full file — slate palette, @theme block, dark-only, typography)
    - /Users/admin/hub/workspace/homelab/apps/admin/components.json (shadcn config — style, baseColor, iconLibrary)
    - /Users/admin/hub/workspace/homelab/apps/admin/components/ui/ (list all primitives via ls)
    - /Users/admin/hub/knowledge/references/frontend-stack-spec.md §3, §4, §5, §10 (what the style spec must NOT re-specify)
    - /Users/admin/hub/knowledge/voidnet/ui-spec.md (top 30 lines — deprecation target)
    - .planning/phases/13-migrate-dashboard-frontend-to-shared-frontend-stack-spec/13-RESEARCH.md lines 455-459 (filename discretion recommendation)
  </read_first>
  <behavior>
    - Spec documents: dark-only class strategy (`<html className="dark">`), slate HSL palette table (copy from admin globals.css @theme block), typography stack (system-ui / font-sans), shadcn style="new-york", baseColor="slate", iconLibrary="lucide", full primitive inventory (exact 19 filenames from admin/components/ui/)
    - Spec explicitly defers layout/code patterns to frontend-stack-spec.md (no overlap)
    - voidnet/ui-spec.md line 1 becomes `> **OBSOLETE** — superseded by ~/hub/knowledge/references/ui-style-spec.md (see Phase 13, D-12)`
  </behavior>
  <action>
    1. Create `/Users/admin/hub/knowledge/references/ui-style-spec.md` (final name at Claude's discretion per D-12; prefer `ui-style-spec.md` to parallel `frontend-stack-spec.md`). Structure:
       - Header: `# Shared UI Style Spec` + purpose sentence + "Source of truth: /Users/admin/hub/workspace/homelab/apps/admin (2026-04-17 snapshot)"
       - §1 Theme strategy: dark-only, `<html className="dark">` at root layout, no light-mode toggle this revision
       - §2 Color palette: copy the `@theme` block from admin/app/globals.css verbatim into a fenced code block (slate HSL values)
       - §3 Typography: system-ui stack (copy from admin globals.css)
       - §4 shadcn config: copy admin/components.json verbatim into a fenced block; note style="new-york", baseColor="slate", iconLibrary="lucide"
       - §5 Component inventory: bullet list of every filename under admin/components/ui/ (alert-dialog.tsx, alert.tsx, avatar.tsx, badge.tsx, button.tsx, card.tsx, dialog.tsx, dropdown-menu.tsx, form.tsx, input.tsx, label.tsx, progress.tsx, select.tsx, skeleton.tsx, sonner.tsx, table.tsx, textarea.tsx, tooltip.tsx — expand by ls at task time)
       - §6 Consumer contract: "Consumers copy `components/ui/*` verbatim; edits are forbidden per D-14 semantics. Deltas must be proposed as updates to this spec file, not downstream divergence."
       - §7 Non-scope: note that runtime/deps/scripts live in frontend-stack-spec.md (no duplication)
       - §8 Change log: 2026-04-17 initial authoring (from homelab/apps/admin snapshot)
    2. Edit `/Users/admin/hub/knowledge/voidnet/ui-spec.md` — prepend these two lines at the top (before existing content):
       ```
       > **OBSOLETE (2026-04-17).** Superseded by /Users/admin/hub/knowledge/references/ui-style-spec.md per Animaya Phase 13 D-12. Do not edit. Voidnet admin will migrate in a separate hub-repo task (deferred).

       ```
       (Keep existing body; do not delete.)
    3. No code; docs-only task.
  </action>
  <verify>
    <automated>test -f /Users/admin/hub/knowledge/references/ui-style-spec.md && grep -q "Source of truth" /Users/admin/hub/knowledge/references/ui-style-spec.md && grep -q "new-york" /Users/admin/hub/knowledge/references/ui-style-spec.md && grep -q "slate" /Users/admin/hub/knowledge/references/ui-style-spec.md &amp;&amp; head -3 /Users/admin/hub/knowledge/voidnet/ui-spec.md | grep -q "OBSOLETE"</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/admin/hub/knowledge/references/ui-style-spec.md` exists with §1–§8 headings
    - Contains literal strings: `Source of truth:`, `new-york`, `slate`, `lucide`
    - Contains all 19 primitive filenames (one per line or in a list)
    - `/Users/admin/hub/knowledge/voidnet/ui-spec.md` first 3 lines contain the string `OBSOLETE`
    - No edits to `homelab/apps/admin` (source of truth; read-only)
  </acceptance_criteria>
  <done>Shared UI style spec exists and is consumable; voidnet/ui-spec.md carries deprecation banner.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold dashboard/ project verbatim from homelab/apps/admin (configs + shadcn primitives)</name>
  <read_first>
    - /Users/admin/hub/workspace/homelab/apps/admin/package.json (full)
    - /Users/admin/hub/workspace/homelab/apps/admin/tsconfig.json
    - /Users/admin/hub/workspace/homelab/apps/admin/eslint.config.mjs
    - /Users/admin/hub/workspace/homelab/apps/admin/postcss.config.mjs
    - /Users/admin/hub/workspace/homelab/apps/admin/bunfig.toml
    - /Users/admin/hub/workspace/homelab/apps/admin/components.json
    - /Users/admin/hub/workspace/homelab/apps/admin/test-setup.ts
    - /Users/admin/hub/workspace/homelab/apps/admin/app/globals.css
    - /Users/admin/hub/workspace/homelab/apps/admin/app/layout.tsx
    - /Users/admin/hub/workspace/homelab/apps/admin/app/error.tsx
    - /Users/admin/hub/workspace/homelab/apps/admin/app/not-found.tsx
    - /Users/admin/hub/workspace/homelab/apps/admin/next.config.mjs (note: drop bun:sqlite webpack shim per PATTERNS.md)
    - /Users/admin/hub/workspace/homelab/apps/admin/lib/utils.ts
    - /Users/admin/hub/workspace/homelab/apps/admin/components/ui/ (all 19 files)
    - .planning/phases/13-migrate-dashboard-frontend-to-shared-frontend-stack-spec/13-PATTERNS.md lines 86-288
  </read_first>
  <action>
    1. Create `dashboard/` at repo root. Do NOT delete `bot/dashboard/` yet (Plan 06 handles cutover).
    2. Copy VERBATIM from `/Users/admin/hub/workspace/homelab/apps/admin/` to `dashboard/`:
       - `tsconfig.json`, `eslint.config.mjs`, `postcss.config.mjs`, `bunfig.toml`, `components.json`, `test-setup.ts`
       - `app/globals.css` → `dashboard/app/globals.css` (spec §4 verbatim)
       - `app/layout.tsx` → `dashboard/app/layout.tsx`
       - `app/error.tsx`, `app/not-found.tsx`
       - `lib/utils.ts` → `dashboard/lib/utils.ts`
       - `components/ui/*.tsx` (all 19 files) → `dashboard/components/ui/` (copy verbatim; no edits per D-14)
    3. Create `dashboard/package.json` by copying admin's verbatim, with these deltas ONLY:
       - `"name": "@animaya/dashboard"`
       - `"dev": "next dev -p 8090 -H 127.0.0.1"` (spec verbatim)
       - `"start": "next start -p 8090 -H 127.0.0.1"` (spec verbatim)
       - `"test": "bun test"` (spec verbatim)
       - Keep dep list byte-identical to admin's (D-13: full Radix set + cva + tailwind-merge + clsx + lucide-react + react-hook-form + zod + @hookform/resolvers + swr + sonner + next-themes + server-only + next@15.5.15 + react@19.2.5 + next-auth@5.0.0-beta.31 + tailwindcss@^4.2.2 + @tailwindcss/postcss + typescript@^6.0.3 + eslint@^10.2.0 + @types/bun)
    4. Create `dashboard/next.config.mjs` from admin's but DROP the `bun:sqlite` webpack shim (lines 16-43 per PATTERNS.md). Keep: `output: 'standalone'`, `reactStrictMode: true`, `poweredByHeader: false`.
    5. Create `dashboard/.gitignore` with: `node_modules/\n.next/\n.env*.local\nplaywright-report/\ntest-results/\n`
    6. Run `cd dashboard && bun install`. Commit the produced `bun.lockb`.
    7. Sanity: `cd dashboard && bunx tsc --noEmit` must pass (zero TS errors — admin's configs are green).
    8. Do NOT create any `app/(auth)/*`, `app/(public)/*`, `app/api/*`, `middleware.ts`, `auth.ts`, or business-logic `lib/*.server.ts` yet. Those are Plans 02–05.
  </action>
  <verify>
    <automated>cd dashboard && bun install --frozen-lockfile && bunx tsc --noEmit && test -f components/ui/button.tsx && test -f components/ui/form.tsx && test -f components/ui/dialog.tsx && grep -q '"next": "15.5.15"' package.json && grep -q '"name": "@animaya/dashboard"' package.json && grep -q '\-p 8090 \-H 127.0.0.1' package.json && grep -q 'output:.*standalone' next.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/package.json` scripts match spec: `-p 8090 -H 127.0.0.1` (both dev and start)
    - All 19 `components/ui/*.tsx` present; byte-equal to admin (verify: `diff -r dashboard/components/ui /Users/admin/hub/workspace/homelab/apps/admin/components/ui` returns empty)
    - `bun install --frozen-lockfile` succeeds (lockfile committed)
    - `bunx tsc --noEmit` exits 0
    - `next.config.mjs` does NOT contain string `bun:sqlite`
    - `app/globals.css` first 5 lines match admin's first 5 lines (spec §4 verbatim)
  </acceptance_criteria>
  <done>Scaffolded Next.js project compiles with zero TS errors; shadcn primitives identical to admin.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Install Playwright + bun test harness and seed Wave 0 fixtures</name>
  <read_first>
    - /Users/admin/hub/workspace/homelab/apps/admin/test-setup.ts (if present — for bun test preload pattern)
    - /Users/admin/hub/workspace/homelab/apps/admin/lib/csrf.server.test.ts (pattern for bun-test shape)
    - .planning/phases/13-migrate-dashboard-frontend-to-shared-frontend-stack-spec/13-RESEARCH.md lines 480-517 (Validation Architecture + Wave 0 Gaps)
    - .planning/phases/13-migrate-dashboard-frontend-to-shared-frontend-stack-spec/13-VALIDATION.md (per-task map section — this task populates it)
  </read_first>
  <action>
    1. `cd dashboard && bun add -d @playwright/test playwright` (pin whatever latest resolves; commit lockfile).
    2. Create `dashboard/playwright.config.ts`:
       ```ts
       import { defineConfig } from "@playwright/test";
       export default defineConfig({
         testDir: "./tests/e2e",
         fullyParallel: false,
         retries: 0,
         use: {
           baseURL: process.env.PW_BASE_URL ?? "http://127.0.0.1:8090",
           trace: "retain-on-failure",
         },
         webServer: process.env.PW_EXTERNAL
           ? undefined
           : {
               command: "bun run start",
               port: 8090,
               reuseExistingServer: !process.env.CI,
               env: { AUTH_SECRET: "test-secret-insecure", DASHBOARD_TOKEN: "test-dash-token", TELEGRAM_BOT_TOKEN: "0:dummy", ANIMAYA_ENGINE_URL: "http://127.0.0.1:8091", OWNER_TELEGRAM_ID: "111111" },
             },
       });
       ```
    3. Create `dashboard/tests/e2e/fixtures.ts`:
       - Export helpers: `mockTelegramWidgetPayload(overrides?)` (constructs a payload with a correctly-HMAC'd `hash` for bot_token `"0:dummy"`), `signedInRequest(page)` (sets DASHBOARD_TOKEN header), `withOwnerFile(ownerId)` (writes a temp OWNER.md path for tests to consume).
       - Keep tests empty — the goal is a working harness. A single smoke spec `tests/e2e/smoke.spec.ts` asserts `await page.goto("/")` returns a non-500 response or a 3xx redirect.
    4. Create `dashboard/bunfig.toml` if not already copied from admin, ensuring `[test] preload = ["./test-setup.ts"]`.
    5. Populate the per-task verification map in `.planning/phases/13-.../13-VALIDATION.md` with the following rows (append under "## Per-Task Verification Map"):
       | Plan.Task | Req | Command |
       | 13-01.1 | D-12 | `grep -q 'OBSOLETE' /Users/admin/hub/knowledge/voidnet/ui-spec.md` |
       | 13-01.2 | D-13,D-14 | `cd dashboard && bunx tsc --noEmit` |
       | 13-01.3 | D-11 | `cd dashboard && bunx playwright test --list` |
       | 13-02.* | D-06,D-07,Telegram HMAC | `cd dashboard && bun test lib/` |
       | 13-03.* | SEC-01,SEC-02 | `cd dashboard && bun test app/api/` |
       | 13-04.* | DASH-01..04 | `cd dashboard && bunx playwright test` |
       | 13-05.* | D-03 | `pytest tests/engine/ -v` |
       | 13-06.* | D-01,D-08 | `rg -n 'itsdangerous\|Jinja2' bot/ || echo ok` + full suite |
    6. Flip `wave_0_complete: true` and `nyquist_compliant: true` in the VALIDATION.md frontmatter.
  </action>
  <verify>
    <automated>cd dashboard && bunx playwright install --with-deps chromium >/dev/null 2>&amp;1 || true; bunx playwright test --list && test -f tests/e2e/fixtures.ts && test -f tests/e2e/smoke.spec.ts && grep -q 'wave_0_complete: true' ../.planning/phases/13-migrate-dashboard-frontend-to-shared-frontend-stack-spec/13-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `bunx playwright test --list` exits 0 and shows at least the smoke spec
    - `dashboard/tests/e2e/fixtures.ts` exports `mockTelegramWidgetPayload`, `signedInRequest`, `withOwnerFile` (grep each name)
    - VALIDATION.md per-task map has rows for plans 01–06
    - VALIDATION.md frontmatter: `wave_0_complete: true`, `nyquist_compliant: true`
  </acceptance_criteria>
  <done>Test harness executes; Wave 0 gates closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → hub/knowledge | Planner writes spec doc; no untrusted input |
| package registry → dashboard/node_modules | `bun install` pulls pinned versions from npm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | bun.lockb integrity | mitigate | commit lockfile; use `--frozen-lockfile` in Plan 06 Dockerfile |
| T-13-02 | Info Disclosure | test AUTH_SECRET leaking | accept | Playwright config uses `"test-secret-insecure"` literal — test-only; never consumed by prod build |
| T-13-03 | Spoofing | spec drift from admin | mitigate | ui-style-spec.md documents the snapshot date; future alignment is a separate phase (deferred per CONTEXT) |
</threat_model>

<verification>
- `cd dashboard && bun install --frozen-lockfile && bunx tsc --noEmit` — clean
- `cd dashboard && bunx playwright test --list` — shows smoke spec
- `diff -r dashboard/components/ui /Users/admin/hub/workspace/homelab/apps/admin/components/ui` — empty
- `test -f /Users/admin/hub/knowledge/references/ui-style-spec.md`
- `head -3 /Users/admin/hub/knowledge/voidnet/ui-spec.md | grep OBSOLETE`
</verification>

<success_criteria>
- Shared UI style spec authored; voidnet/ui-spec.md deprecated
- dashboard/ scaffolded with all spec-§2 deps, all 19 shadcn primitives verbatim, TS-clean
- Playwright + bun test harness runnable; VALIDATION.md map populated
</success_criteria>

<output>
After completion, create `.planning/phases/13-migrate-dashboard-frontend-to-shared-frontend-stack-spec/13-01-SUMMARY.md`.
</output>
