---
phase: 13
plan: 05
type: execute
wave: 4
depends_on: [13-03, 13-04]
files_modified:
  - dashboard/app/(auth)/layout.tsx
  - dashboard/app/(auth)/page.tsx
  - dashboard/app/(auth)/chat/page.tsx
  - dashboard/app/(auth)/_components/chat-panel.tsx
  - dashboard/app/(auth)/_components/hub-tree.tsx
  - dashboard/app/(auth)/_components/tool-use-event.tsx
  - dashboard/app/(auth)/_lib/use-sse.ts
  - dashboard/app/(auth)/modules/page.tsx
  - dashboard/app/(auth)/modules/[name]/page.tsx
  - dashboard/app/(auth)/bridge/page.tsx
  - dashboard/app/(auth)/bridge/_components/config-form.tsx
  - dashboard/app/(public)/layout.tsx
  - dashboard/app/(public)/login/page.tsx
  - dashboard/app/(public)/403/page.tsx
  - dashboard/components/layout/topbar.tsx
  - dashboard/components/layout/sidebar.tsx
  - dashboard/components/layout/nav-items.ts
  - dashboard/tests/e2e/auth.spec.ts
  - dashboard/tests/e2e/dashboard-token.spec.ts
  - dashboard/tests/e2e/surface.spec.ts
  - dashboard/tests/e2e/chat.spec.ts
  - dashboard/tests/e2e/hub-tree.spec.ts
  - dashboard/tests/e2e/modules.spec.ts
  - dashboard/tests/e2e/bridge.spec.ts
autonomous: true
requirements: [D-01, D-05, D-06, D-07, D-10, D-15, DASH-01, DASH-02, DASH-03, SEC-01, SEC-02]
tags: [pages, ui, react, e2e, playwright]
must_haves:
  truths:
    - "Every current dashboard page has a React replacement: home, modules, module_detail, bridge_config, login, 403"
    - "DASH-01: /chat page renders unified chat + Hub file tree in a split layout"
    - "DASH-02: chat messages stream via SSE; tool-use events render inline as shadcn Cards with lucide icons"
    - "DASH-03: hub tree collapses, hides dotfiles by default, toggle persists in localStorage"
    - "Login page embeds Telegram Login Widget; successful widget callback POSTs to /api/auth/callback/telegram and signs the user in"
    - "Non-owner Telegram IDs land on /403 (D-07)"
    - "DASHBOARD_TOKEN header grants access without a session (D-06)"
    - "Playwright E2E suite covers auth / token bypass / chat / tree / modules / bridge / 403"
  artifacts:
    - path: "dashboard/app/(auth)/chat/page.tsx"
      provides: "Unified chat + tree page"
      contains: "HubTree"
    - path: "dashboard/app/(auth)/_components/chat-panel.tsx"
      provides: "SSE chat renderer with tool-use inline events"
      contains: "useSSE"
    - path: "dashboard/app/(public)/login/page.tsx"
      provides: "Telegram Login Widget embed"
      contains: "telegram-widget.js"
    - path: "dashboard/tests/e2e/auth.spec.ts"
      provides: "Owner-only sign-in E2E"
  key_links:
    - from: "dashboard/app/(auth)/chat/page.tsx"
      to: "/api/chat/stream"
      via: "use-sse hook POSTs to streaming route"
      pattern: "/api/chat/stream"
    - from: "dashboard/app/(auth)/_components/hub-tree.tsx"
      to: "/api/hub/tree"
      via: "SWR"
      pattern: "/api/hub/tree"
    - from: "dashboard/app/(auth)/bridge/_components/config-form.tsx"
      to: "zod schemas in lib/schemas.ts"
      via: "@hookform/resolvers/zod"
      pattern: "zodResolver"
---

<objective>
Wave 4 pages + layouts + E2E: port every current FastAPI/Jinja page into React (parity per D-10), implement the new unified chat+tree page (DASH-01), Telegram Login Widget embed, and lock the Playwright E2E suite covering auth + SSE + tree + modules + bridge.

Purpose: the browser can now navigate the entire dashboard in Next.js; legacy Jinja is unused (but not yet deleted — Plan 06 does cutover).
Output: full app-router page tree, layout chrome, SSE client hook + chat panel + hub tree components, 7 Playwright specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-migrate-dashboard-frontend-to-shared-frontend-stack-spec/13-CONTEXT.md
@.planning/phases/13-migrate-dashboard-frontend-to-shared-frontend-stack-spec/13-RESEARCH.md
@.planning/phases/13-migrate-dashboard-frontend-to-shared-frontend-stack-spec/13-PATTERNS.md
@/Users/admin/hub/workspace/homelab/apps/admin/app/(auth)/layout.tsx
@/Users/admin/hub/workspace/homelab/apps/admin/app/(auth)/page.tsx
@/Users/admin/hub/workspace/homelab/apps/admin/app/(auth)/tokens/page.tsx
@/Users/admin/hub/workspace/homelab/apps/admin/app/(public)/layout.tsx
@/Users/admin/hub/workspace/homelab/apps/admin/app/(public)/login/page.tsx
@/Users/admin/hub/workspace/homelab/apps/admin/app/(public)/403/page.tsx
@/Users/admin/hub/workspace/homelab/apps/admin/components/layout/topbar.tsx
@/Users/admin/hub/workspace/homelab/apps/admin/components/layout/sidebar.tsx
@/Users/admin/hub/workspace/animaya/bot/dashboard/templates/
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Chrome + public pages (layouts, login with Telegram Widget, 403, home)</name>
  <read_first>
    - homelab/apps/admin/app/(auth)/layout.tsx (copy verbatim)
    - homelab/apps/admin/app/(public)/login/page.tsx (template; swap GH signIn for Telegram Widget per PATTERNS.md §login)
    - homelab/apps/admin/app/(public)/403/page.tsx (copy verbatim)
    - homelab/apps/admin/components/layout/{topbar,sidebar}.tsx (copy verbatim; replace nav-items)
    - bot/dashboard/templates/home.html (for parity check — what info the home page currently shows)
  </read_first>
  <action>
    1. Copy `app/(auth)/layout.tsx`, `app/(public)/layout.tsx`, `app/(public)/403/page.tsx` verbatim from admin.
    2. Copy `components/layout/topbar.tsx` + `sidebar.tsx` verbatim. Create `components/layout/nav-items.ts` with Animaya nav: Home `/`, Chat `/chat`, Modules `/modules`, Bridge `/bridge`.
    3. Build `dashboard/app/(public)/login/page.tsx` — Telegram Login Widget embed (PATTERNS.md lines 266-273):
       ```tsx
       export default function Login() {
         const username = process.env.NEXT_PUBLIC_TELEGRAM_BOT_USERNAME;
         return (
           <main className="flex min-h-screen items-center justify-center">
             <div className="flex flex-col items-center gap-6">
               <h1 className="text-2xl font-semibold">Sign in to Animaya</h1>
               {username ? (
                 <script async src="https://telegram.org/js/telegram-widget.js?22"
                   data-telegram-login={username} data-size="large"
                   data-auth-url="/api/auth/callback/telegram"
                   data-request-access="write" />
               ) : <p className="text-sm opacity-70">Telegram widget unavailable (NEXT_PUBLIC_TELEGRAM_BOT_USERNAME unset).</p>}
             </div>
           </main>
         );
       }
       ```
       Note: the widget's `data-auth-url` posts `{id, first_name, username, auth_date, hash}` via form POST; next-auth Credentials provider picks it up via the callback URL. If next-auth v5 Credentials flow requires a client-side `signIn("telegram", payload)` instead, add a small `<Script>` + wrapper that captures the widget's `onTelegramAuth` global and calls `signIn`. Implementer: verify A7 against next-auth@5.0.0-beta.31 docs FIRST; pick whichever shape beta.31 supports. Document the choice in a code comment.
    4. Build `app/(auth)/page.tsx` (Home): status strip (server-fetched via `engineFetch("/engine/status")` if such an endpoint is cheap — else omit), quick links to /chat, /modules, /bridge. Simple Card grid. Keep parity with current `bot/dashboard/templates/home.html` (inventory info).
    5. Run: `cd dashboard && bun run build` — must succeed.
  </action>
  <verify>
    <automated>cd dashboard && bun run build &amp;&amp; test -f app/(auth)/layout.tsx &amp;&amp; test -f app/(public)/login/page.tsx &amp;&amp; test -f app/(public)/403/page.tsx &amp;&amp; grep -q 'telegram-widget.js' app/(public)/login/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `bun run build` succeeds (produces `.next/standalone`)
    - Login page contains `telegram-widget.js` script tag
    - 403 page matches admin's structure
    - Topbar + Sidebar render with animaya nav items
  </acceptance_criteria>
  <done>Chrome + public pages shipped; build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Chat + Hub tree (DASH-01/02/03) + Modules + Bridge pages with RHF+zod forms (D-15)</name>
  <read_first>
    - homelab/apps/admin/app/(auth)/tokens/page.tsx (list view with SWR — analog for modules list)
    - homelab/apps/admin/components/ui/form.tsx (rhf + zod wiring pattern)
    - bot/dashboard/templates/modules.html, module_detail.html, bridge_config.html (parity targets)
    - bot/dashboard/forms.py (fields/types to preserve in zod shapes)
    - .planning/phases/13-migrate-dashboard-frontend-to-shared-frontend-stack-spec/13-RESEARCH.md Open Question 1 (markdown renderer decision)
  </read_first>
  <action>
    1. **use-sse hook** — `dashboard/app/(auth)/_lib/use-sse.ts`:
       - `useSSE({url, payload})` → `{events, send, status}` where `events` is an append-only array of parsed `data: {...}` frames
       - Implementation: `fetch(url, {method:"POST", body: JSON.stringify(payload), headers: {"content-type":"application/json", "x-csrf-token": cookieAnCsrf}})` then read `response.body.getReader()` as text; split on `\n\n`; parse each `data: ` line with JSON.parse; ignore `:ping`
       - Reconnect on close with exponential backoff capped at 30s
       - Expose `status: "idle" | "streaming" | "error" | "closed"`
    2. **tool-use-event component** — `_components/tool-use-event.tsx`:
       - Props: `{type, tool?, input?, output?, content?}`
       - `text` events render markdown (decide renderer: add `react-markdown` + `remark-gfm` to deps — minor spec extension, document in ui-style-spec.md per Open Q1)
       - `tool_use` / `tool_result` render as `<Card>` with lucide icon matching tool name
    3. **chat-panel component** — `_components/chat-panel.tsx`:
       - Left column: message list + input (Textarea from ui + Send button)
       - On submit: call `useSSE` with POST to `/api/chat/stream` body `{message}`
       - Append each event to the rolling transcript; scroll to bottom on update
       - Empty state: "Start a conversation."
    4. **hub-tree component** — `_components/hub-tree.tsx`:
       - Client component using SWR `/api/hub/tree?path=<rel>&show_hidden=<bool>`
       - Collapsible dir nodes (open state persisted in `localStorage["animaya.treeOpen"]`)
       - Dotfile toggle (persisted in `localStorage["animaya.treeShowHidden"]`, default false)
       - Click file → fetch `/api/hub/file?path=...` and open right-pane read-only viewer (split layout lives in chat/page.tsx)
    5. **chat page** — `app/(auth)/chat/page.tsx`:
       - Split layout: chat-panel (flex-1) | hub-tree (w-80, collapsible sidebar)
       - Server component checks `auth()`; redirects to `/login` if no session and no DASHBOARD_TOKEN header
    6. **modules list** — `app/(auth)/modules/page.tsx`:
       - SWR `/api/modules`; render Card per module with Install/Uninstall Button
       - Mutation: `fetch("/api/modules/:name/install", {method:"POST", headers:{"x-csrf-token":...}})`; revalidate via `mutate`
       - Toast on success/error via `sonner`
    7. **module detail** — `app/(auth)/modules/[name]/page.tsx`:
       - Config form using rhf + zod (`ModuleConfigSchema`); renders inputs dynamically from module's advertised fields (fetched separately or shipped as part of module DTO)
       - Submit PUTs `/api/modules/:name/config`
    8. **bridge page** — `app/(auth)/bridge/page.tsx` + `_components/config-form.tsx`:
       - Shows current claim status (owner_id, claim_code presence — redacted), toggle, policy
       - Actions: Claim (POST), Revoke (POST), Regenerate (POST), Toggle (PUT), Policy select (PUT)
       - All via rhf + zod (`BridgeTogglePayload`, `BridgePolicyPayload`)
    9. `cd dashboard && bun run build` — must succeed. No ESLint errors.
  </action>
  <verify>
    <automated>cd dashboard && bun run build &amp;&amp; bunx eslint . &amp;&amp; test -f app/(auth)/chat/page.tsx &amp;&amp; test -f app/(auth)/_components/chat-panel.tsx &amp;&amp; test -f app/(auth)/_components/hub-tree.tsx &amp;&amp; test -f app/(auth)/_lib/use-sse.ts &amp;&amp; test -f app/(auth)/modules/page.tsx &amp;&amp; test -f app/(auth)/bridge/page.tsx &amp;&amp; grep -q 'zodResolver' app/(auth)/bridge/_components/config-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Build green; ESLint clean
    - All 6 new page files exist
    - chat-panel imports `useSSE`; chat page renders hub-tree alongside
    - bridge config-form uses `zodResolver` (D-15)
    - modules list uses `swr`
  </acceptance_criteria>
  <done>All pages render; full feature-parity plus new unified chat+tree.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Playwright E2E suite (7 specs) covering auth, token bypass, SSE, tree, modules, bridge, surface</name>
  <read_first>
    - .planning/phases/13-migrate-dashboard-frontend-to-shared-frontend-stack-spec/13-RESEARCH.md §Validation Architecture (test matrix)
    - dashboard/tests/e2e/fixtures.ts (from Plan 01)
  </read_first>
  <action>
    Each spec boots the Playwright `webServer` (from Plan 01 playwright.config.ts) with env-provided `DASHBOARD_TOKEN=test-dash-token`, `OWNER_TELEGRAM_ID=111111`, and a mocked engine (a tiny Bun-started sidecar that mimics `/engine/*` — include `dashboard/tests/e2e/_mock-engine.ts` + a `globalSetup` that starts it on 127.0.0.1:8091). Specs:

    1. `surface.spec.ts` (D-01): hitting `http://127.0.0.1:8091/engine/modules` directly from the test runner via Node fetch → EXPECTED to work (loopback); but hitting `/engine/modules` on :8090 → 404. Asserts Next.js has no passthrough to FastAPI on the public port.
    2. `auth.spec.ts` (D-05, D-07): unauth visit to `/` → 302 to `/login`. Simulate valid widget payload via direct POST to `/api/auth/callback/credentials` with a payload signed by the test's known bot_token (fixtures.mockTelegramWidgetPayload). Owner match → lands on `/`. Owner mismatch → `/403`.
    3. `dashboard-token.spec.ts` (D-06): `page.setExtraHTTPHeaders({"x-dashboard-token":"test-dash-token"})`; visit `/modules` → 200 without signing in. Bad token → 302 to /login.
    4. `chat.spec.ts` (DASH-02): navigate to `/chat`, type "hi", submit; mock engine emits `text`, `tool_use`, `tool_result`, `end`. Assert tool-use Card renders inline between text chunks. Assert heartbeat doesn't visually appear as a message. Assert reconnect after forced stream close.
    5. `hub-tree.spec.ts` (DASH-03): tree shows `.git` ONLY when "show hidden" toggle enabled; clicking dir expands (persists on reload via localStorage); clicking file loads right-pane viewer.
    6. `modules.spec.ts`: mock engine returns 2 modules; clicking Install POSTs `/api/modules/:name/install` (assert CSRF header present; assert redacted DTO — no `bot_token` in the response visible in dev tools network log).
    7. `bridge.spec.ts`: claim → expect code rendered; revoke → button disables; policy select → PUT with correct zod shape.

    Run: `cd dashboard && bunx playwright install chromium && bunx playwright test`.
  </action>
  <verify>
    <automated>cd dashboard &amp;&amp; bunx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - All 7 spec files exist and pass
    - `playwright-report/` shows 0 failures
    - Mock engine sidecar exits cleanly after global teardown
    - Every spec maps to a requirement ID in the per-task validation map
  </acceptance_criteria>
  <done>End-to-end coverage for parity + owner-gate + SSE + tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ Next.js | covered by middleware; client code never holds secrets |
| localStorage | untrusted; used only for UI preference state (tree expansion) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-40 | XSS | markdown renderer in chat-panel | mitigate | `react-markdown` defaults escape HTML; no `rehype-raw`; no `dangerouslySetInnerHTML` |
| T-13-41 | Spoofing | Telegram widget origin bypass | accept | Telegram validates origin server-side before issuing callback; admin must `@BotFather /setdomain` at install (RESEARCH Pitfall 1 — documented as install-step concern) |
| T-13-42 | Info Disclosure | localStorage leaking sensitive state | accept | only UI prefs stored (tree open/hidden toggle); no tokens, no chat content |
| T-13-43 | CSRF | SSE POST forged from another origin | mitigate | `useSSE` attaches `x-csrf-token` from `an-csrf` cookie; server verifyCsrf enforces double-submit |
| T-13-44 | Info Disclosure | client-side fetch leaking bot_token | mitigate | response DTOs already redacted (Plan 03); Playwright test asserts token string never in network payloads |
</threat_model>

<verification>
- `cd dashboard && bun run build` — green
- `cd dashboard && bunx eslint .` — no errors
- `cd dashboard && bunx playwright test` — 7 specs pass
- Manual: `bun run start` + browser visit `/chat` with DASHBOARD_TOKEN → streaming roundtrip with mock engine
</verification>

<success_criteria>
- All parity pages ported
- DASH-01/02/03 satisfied in Next.js (ahead of Phase 12)
- Playwright suite green
- Telegram Login Widget integrated with next-auth Credentials flow
</success_criteria>

<output>
After completion, create `.planning/phases/13-migrate-dashboard-frontend-to-shared-frontend-stack-spec/13-05-SUMMARY.md`.
</output>
