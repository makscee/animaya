---
phase: 07-close-v1-audit-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-telegram-bridge/02-VERIFICATION.md
autonomous: true
requirements: [TELE-01, TELE-02, TELE-03, TELE-04, TELE-05]
gap_closure: true
tags: [verification, bookkeeping, retroactive]

must_haves:
  truths:
    - "02-VERIFICATION.md exists at .planning/phases/02-telegram-bridge/02-VERIFICATION.md"
    - "File contains verdict lines for all five requirement IDs TELE-01..TELE-05"
    - "Each requirement has SATISFIED or UNSATISFIED verdict with file:line evidence"
    - "Frontmatter contains phase, verified, status, score fields matching 01-VERIFICATION.md shape"
  artifacts:
    - path: ".planning/phases/02-telegram-bridge/02-VERIFICATION.md"
      provides: "Retroactive verification of TELE-01..05 against shipped bridge code"
      contains: "TELE-01"
    - path: ".planning/phases/02-telegram-bridge/02-VERIFICATION.md"
      contains: "TELE-05"
  key_links:
    - from: "02-VERIFICATION.md"
      to: "bot/bridge/telegram.py"
      via: "file:line evidence in Observable Truths / Requirements Coverage table"
      pattern: "bot/bridge/telegram\\.py"
    - from: "02-VERIFICATION.md"
      to: "bot/bridge/formatting.py"
      via: "chunking + HTML conversion evidence"
      pattern: "bot/bridge/formatting\\.py"
---

<objective>
Create `.planning/phases/02-telegram-bridge/02-VERIFICATION.md` retroactively by running goal-backward verification of requirements TELE-01..TELE-05 against the shipped bridge code. Close the audit gap that flagged Phase 02 as UNVERIFIED.

Purpose: The audit `.planning/v1.0-MILESTONE-AUDIT.md` flagged Phase 02 as UNVERIFIED (no VERIFICATION.md artifact despite shipped code). Re-running `/gsd-audit-milestone 1.0` will reject milestone archival until this artifact exists.

Output: One markdown file — `02-VERIFICATION.md` — with per-requirement SATISFIED/UNSATISFIED verdicts, evidence anchored to file:line in the bridge code, and frontmatter matching the style of `01-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/07-close-v1-audit-gaps/07-CONTEXT.md

# Style exemplars — follow the structure (frontmatter + Observable Truths + Requirements Coverage + Key Links)
@.planning/phases/01-install-foundation/01-VERIFICATION.md
@.planning/phases/04-first-party-modules/04-VERIFICATION.md

# Phase 02 source of truth (plans + summaries describe what shipped)
@.planning/phases/02-telegram-bridge/02-01-PLAN.md
@.planning/phases/02-telegram-bridge/02-01-SUMMARY.md
@.planning/phases/02-telegram-bridge/02-02-PLAN.md
@.planning/phases/02-telegram-bridge/02-02-SUMMARY.md

# Shipped code under verification
@bot/bridge/telegram.py
@bot/bridge/formatting.py
@bot/claude_query.py
@bot/main.py

# Tests that prove behavior
@tests/bridge/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spawn gsd-verifier agent — produce 02-VERIFICATION.md for TELE-01..05</name>
  <files>.planning/phases/02-telegram-bridge/02-VERIFICATION.md</files>
  <read_first>
    - .planning/v1.0-MILESTONE-AUDIT.md (gap inventory — Phase 02 row)
    - .planning/REQUIREMENTS.md (TELE-01..05 definitions + success criteria)
    - .planning/ROADMAP.md (Phase 2 success criteria)
    - .planning/phases/01-install-foundation/01-VERIFICATION.md (style exemplar — copy frontmatter shape + table structure)
    - .planning/phases/04-first-party-modules/04-VERIFICATION.md (style exemplar — requirements coverage table)
    - .planning/phases/02-telegram-bridge/02-01-SUMMARY.md and 02-02-SUMMARY.md (what shipped per plan)
    - bot/bridge/telegram.py (full file — handler, streaming throttle, lock, chunking dispatch, error surfacing)
    - bot/bridge/formatting.py (full file — markdown→HTML, chunking)
    - bot/claude_query.py (full file — options builder used by bridge)
    - bot/main.py (bridge wiring — app.build(), message_handler registration)
    - tests/bridge/ (tests that demonstrate behavior)
  </read_first>
  <action>
Spawn the `gsd-verifier` agent with this prompt:

"""
Produce `.planning/phases/02-telegram-bridge/02-VERIFICATION.md` by verifying TELE-01..TELE-05 against shipped code.

Requirement → evidence mapping (hunt-list):
- TELE-01 (Streaming Claude responses, not batched): find the async iteration in `bot/bridge/telegram.py` that consumes Claude SDK stream and progressively edits the Telegram message; record file:line
- TELE-02 (Immediate ack / no Telegram retry / no duplicate): find the per-user asyncio.Lock pattern + typing indicator context manager in `bot/bridge/telegram.py`; record file:line
- TELE-03 (Typing indicator while processing): find `_typing_loop` asynccontextmanager or equivalent; record file:line
- TELE-04 (Long responses split into multiple messages): find chunking in `bot/bridge/formatting.py` + `TG_MAX_LEN` constant + the sequential-send loop in telegram.py; record file:line
- TELE-05 (Errors surface as user-visible Telegram message): find try/except wrapping the Claude query and the fallback `send_message` / `edit_message_text` on exception; record file:line

For each requirement:
- Status: SATISFIED | UNSATISFIED | PARTIAL
- Evidence: file:line references (2-4 lines of quoted code max per requirement)
- Tests: name the pytest file(s) under tests/bridge/ that exercise the behavior

Output structure (match `01-VERIFICATION.md` exactly):
1. Frontmatter: phase, verified (ISO date), status (passed|human_needed|failed), score (N/5), overrides_applied: 0, re_verification: false
2. `# Phase 2: Telegram Bridge — Verification Report` heading
3. `## Goal Achievement` with Observable Truths table (5 rows derived from ROADMAP Phase 2 success criteria)
4. `### Requirements Coverage` table (5 rows, one per TELE-XX) with Status and Evidence columns
5. `### Key Link Verification` table (bridge → claude_query, bridge → formatting, main.py → bridge wiring)
6. `### Behavioral Spot-Checks` table with pytest commands actually runnable
7. If any known-deferred behavior (streaming double-bubble bug per audit tech_debt) surfaces, list under `## Known Gaps` with reference to audit entry — do NOT mark the requirement UNSATISFIED solely for this deferred bug

Do NOT run tests you cannot afford; cite existing test files + counts from 02-0{1,2}-SUMMARY.md.

Write the file atomically. Do not modify any other file.
"""

Verify the verifier's output before accepting:
1. File exists at `.planning/phases/02-telegram-bridge/02-VERIFICATION.md`
2. Grep the file for each of `TELE-01`, `TELE-02`, `TELE-03`, `TELE-04`, `TELE-05` — all must appear
3. Frontmatter fields present: phase, verified, status, score
4. No placeholder `TODO` or `XXX` strings remain
  </action>
  <verify>
    <automated>
test -f .planning/phases/02-telegram-bridge/02-VERIFICATION.md && \
  for id in TELE-01 TELE-02 TELE-03 TELE-04 TELE-05; do \
    grep -q "$id" .planning/phases/02-telegram-bridge/02-VERIFICATION.md || { echo "missing $id"; exit 1; }; \
  done && \
  grep -q "^phase:" .planning/phases/02-telegram-bridge/02-VERIFICATION.md && \
  grep -q "^status:" .planning/phases/02-telegram-bridge/02-VERIFICATION.md && \
  ! grep -qE "TODO|XXX|FIXME" .planning/phases/02-telegram-bridge/02-VERIFICATION.md
    </automated>
  </verify>
  <done>
`02-VERIFICATION.md` exists with frontmatter + all 5 TELE-XX verdicts with file:line evidence anchored to shipped bridge code. No placeholder strings. Matches style of `01-VERIFICATION.md`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Retroactive documentation only — no new attack surface introduced. This plan produces a markdown artifact describing already-shipped behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | I (Information Disclosure) | VERIFICATION.md content | accept | Audit artifact is intended for repo-local reading; no secrets quoted. Verifier instructed to quote code, not .env contents. |
</threat_model>

<verification>
- `02-VERIFICATION.md` exists and contains all 5 TELE-XX verdicts
- Evidence anchored to `bot/bridge/telegram.py`, `bot/bridge/formatting.py`, `bot/claude_query.py` file:line refs
- File matches style of `01-VERIFICATION.md` (frontmatter + Goal Achievement + Requirements Coverage + Key Link + Spot-Checks sections)
</verification>

<success_criteria>
Re-running `/gsd-audit-milestone 1.0` no longer flags Phase 02 as `verification_status: missing`. The file contains grep-able strings `TELE-01` through `TELE-05` with explicit status.
</success_criteria>

<output>
After completion, create `.planning/phases/07-close-v1-audit-gaps/07-01-SUMMARY.md`.
</output>
