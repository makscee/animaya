---
phase: 05
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - bot/dashboard/jobs.py
  - bot/dashboard/modules_view.py
  - bot/dashboard/module_routes.py
  - bot/dashboard/templates/modules.html
  - bot/dashboard/templates/module_detail.html
  - bot/dashboard/templates/_fragments/module_card.html
  - bot/dashboard/templates/_fragments/module_card_running.html
  - bot/dashboard/templates/_fragments/module_card_failed.html
  - bot/dashboard/templates/_fragments/conflict_toast.html
  - tests/dashboard/test_jobs.py
  - tests/dashboard/test_modules.py
autonomous: true
requirements:
  - DASH-04
  - DASH-05
tags: [dashboard, modules, install, jobs, htmx]

must_haves:
  truths:
    - "GET /modules (authenticated owner) renders Installed and Available module grids from registry.json + modules/ directory"
    - "GET /modules/{name} renders module detail page with metadata and (if config_schema present) a form placeholder"
    - "POST /modules/{name}/install enqueues async install job, returns module_card_running fragment with hx-trigger=every 1s"
    - "POST /modules/{name}/uninstall enqueues async uninstall job, same fragment flow"
    - "GET /modules/{name}/job/{job_id} returns running/done/failed fragment depending on JobState"
    - "Single global asyncio.Lock enforces one-install-at-a-time; concurrent POST returns 409 with conflict toast fragment"
    - "Failure fragment shows last ~50 log lines in <details> + rollback badge (clean | dirty with leaked paths)"
    - "Finished jobs retained 10 minutes, then garbage-collected"
    - "Install/uninstall emits events: modules.install info on start, modules.install error with log on failure"
  artifacts:
    - path: "bot/dashboard/jobs.py"
      provides: "JobState dataclass + start_install + start_uninstall + get_job + _gc"
      contains: "asyncio.Lock"
      min_lines: 130
    - path: "bot/dashboard/modules_view.py"
      provides: "available() + installed() + describe(name) helpers"
      contains: "def available"
      min_lines: 50
    - path: "bot/dashboard/module_routes.py"
      provides: "register(app, templates) for /modules + /modules/{name} + install/uninstall POST + job GET"
      contains: "def register"
      min_lines: 100
  key_links:
    - from: "bot/dashboard/jobs.py"
      to: "bot.modules.install"
      via: "asyncio.to_thread"
      pattern: "asyncio.to_thread"
    - from: "bot/dashboard/module_routes.py"
      to: "bot/dashboard/jobs.py"
      via: "start_install, start_uninstall, get_job"
      pattern: "from bot.dashboard.jobs import"
    - from: "bot/dashboard/jobs.py"
      to: "bot/events.py:emit"
      via: "direct import"
      pattern: "from bot.events import emit"
---

<objective>
Deliver DASH-04 + DASH-05: the `/modules` list, `/modules/{name}` detail page, and the async install/uninstall job runner that Plan 03's app factory auto-registers. Integration surface is exclusively bot.modules (install, uninstall, list_installed, get_entry, read_registry) wrapped in an asyncio job runner + HTMX polling fragments.

Purpose: D-07 async + polling, D-08 single-lock + 409, D-09 failure UI with rollback badge, D-10 in-process job dict with 10-min retention, D-15 page URLs, D-16 HTMX fragment swaps.

Output: Working modules browse + install/uninstall UX with live 1-second polling during jobs, green + red fragments, 409 toast, rollback classification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-web-dashboard/05-CONTEXT.md
@.planning/phases/05-web-dashboard/05-RESEARCH.md
@.planning/phases/05-web-dashboard/05-UI-SPEC.md
@bot/modules/__init__.py
@bot/modules/lifecycle.py
@bot/modules/registry.py
@bot/modules/manifest.py
@bot/dashboard/app.py
@bot/dashboard/deps.py
@bot/events.py
@tests/dashboard/conftest.py
@CLAUDE.md

Phase 3 module API contract (VERIFIED from bot/modules/__init__.py):

- install(module_dir: Path, hub_dir: Path, *, config: dict | None = None) -> dict
  raises: ValueError, RuntimeError, FileNotFoundError
- uninstall(name: str, hub_dir: Path, module_dir: Path) -> None
  raises: KeyError, ValueError, RuntimeError
- list_installed(hub_dir: Path) -> list[str]
- get_entry(hub_dir: Path, name: str) -> dict | None
- read_registry(hub_dir: Path) -> dict with {"modules": [entry, ...]}
- validate_manifest(module_dir: Path) -> ModuleManifest (pydantic model with fields: name, version, manifest_version, description, owned_paths, scripts, depends, config_schema, prompt_file)

Entry schema (from bot/modules/lifecycle.py line 183-193):
  {"name","version","manifest_version","installed_at","config","depends","module_dir"}

Modules are discovered from DEFAULT_MODULES_ROOT = repo_root/modules. Per plan, support custom modules root via env ANIMAYA_MODULES_DIR; default to bot.modules.lifecycle.DEFAULT_MODULES_ROOT.

Interfaces this plan creates (used by Plan 06 and Plan 03 factory):

- bot.dashboard.module_routes.register(app, templates) — idempotent
- bot.dashboard.jobs.JobState (dataclass) — attributes id, op, module, status, started, finished, log_lines, error, rollback, leaked_paths
- bot.dashboard.jobs.start_install(module_name, module_dir, hub_dir, config=None) -> JobState
- bot.dashboard.jobs.start_uninstall(name, hub_dir, module_dir) -> JobState
- bot.dashboard.jobs.get_job(job_id) -> JobState | None
- bot.dashboard.jobs.InProgressError (raised when lock already held)

Concurrency: one global asyncio.Lock at module level. On second request, start_* raises InProgressError; route handler returns 409 with conflict toast fragment.

Log capture: bot.modules.install/uninstall currently only log via Python logger and do NOT return stdout/stderr. The job runner captures logs by installing a transient logging.Handler that buffers records tagged with "bot.modules.*" while the job runs (bounded to last 200 records). On failure, last 50 lines go into the failure fragment. Rollback classification: after a failed install, inspect manifest.owned_paths — any path still present under hub_dir => rollback="dirty" with leaked paths; empty => rollback="clean".
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for jobs + modules endpoints (RED)</name>
  <files>tests/dashboard/test_jobs.py, tests/dashboard/test_modules.py</files>
  <read_first>
    - tests/dashboard/conftest.py + _helpers.py
    - bot/modules/lifecycle.py install/uninstall signatures and error types
    - bot/modules/manifest.py ModuleManifest schema
    - tests/modules/conftest.py (existing fixture that seeds modules/ directory — reuse the pattern)
    - .planning/phases/05-web-dashboard/05-UI-SPEC.md Install/Uninstall Job UX
  </read_first>
  <behavior>
    tests/dashboard/test_jobs.py (8 tests, async):
      - test_start_install_returns_running_job — fake module dir with valid manifest + install.sh that exits 0; start_install returns JobState with status "running"; after awaiting briefly status becomes "done"; hub registry has entry
      - test_start_install_failure_captures_log_and_rollback — install.sh exits 1; job.status becomes "failed"; job.error non-empty; job.log_lines non-empty; job.rollback is "clean" (since uninstall.sh ran and owned_paths absent)
      - test_start_install_failure_dirty_rollback — install.sh exits 1 AND leaves a file under owned_paths (echo x > owned.txt); rollback is "dirty" and leaked_paths lists the leaked relpath
      - test_second_install_while_running_raises_InProgressError — start first install with slow install.sh (sleep 1); immediately calling start_install again raises InProgressError
      - test_start_uninstall_clears_entry — pre-seed entry (via install), then start_uninstall returns JobState reaching status "done"; registry lookup returns None
      - test_get_job_returns_None_after_eviction — monkeypatch _JOB_RETENTION = 0; after finish + _gc call, get_job(job_id) returns None
      - test_install_does_not_block_event_loop — assert that during a 300ms install.sh sleep, a concurrent asyncio sleep of 100ms completes (proves asyncio.to_thread wraps the subprocess)
      - test_events_emitted_on_install_and_failure — spy bot.events.emit via monkeypatch; a successful install emits at least one ("info", "modules.install", ...) record; failure emits ("error", "modules.install", ...) with details.log populated

    tests/dashboard/test_modules.py (9 tests):
      - test_modules_list_requires_owner — GET /modules no cookie -> 302 /login
      - test_modules_list_shows_available_and_installed — seed modules/foo/ + modules/bar/ each with valid manifest + install.sh; install foo via bot.modules.install; GET /modules authenticated -> 200, body contains "foo" in Installed grid, "bar" in Available grid
      - test_modules_detail_shows_metadata — GET /modules/foo -> 200 body contains foo version, description, owned_paths
      - test_modules_detail_404_for_unknown — GET /modules/nope -> 404 with friendly HTML
      - test_post_install_returns_running_fragment — POST /modules/bar/install authenticated -> 200, body contains hx-trigger="every 1s" and "Installing"
      - test_job_endpoint_returns_done_fragment — POST /modules/bar/install then poll GET /modules/bar/job/{id} until status done -> body contains badge-green "Installed" AND omits hx-trigger (stop polling)
      - test_job_endpoint_returns_failed_fragment — seed module with failing install.sh; poll until status failed -> body contains "Install failed" + <details> + rollback badge
      - test_second_install_returns_409_conflict_toast — install foo (blocks), immediately POST /modules/bar/install -> 409 with "Another module operation in progress"
      - test_post_uninstall_installed_module — install foo; POST /modules/foo/uninstall -> poll to done; GET /modules shows foo in Available (not Installed)
  </behavior>
  <action>
    Create tests/dashboard/test_jobs.py. Use pytest.mark.asyncio (auto mode enabled in pyproject).

    Helper: create fake module directories under tmp_path / "modules" / name / with:
      - manifest.json (name, version "0.1.0", manifest_version 1, description, owned_paths=[relative path or []], scripts {install "install.sh", uninstall "uninstall.sh"}, depends=[], config_schema=null, prompt_file "prompt.md")
      - install.sh (#!/usr/bin/env bash; exit 0) executable
      - uninstall.sh (#!/usr/bin/env bash; exit 0) executable
      - prompt.md ("module prompt")
    Use shutil.copyfile + Path.chmod(0o755) to make scripts executable.

    For the "slow install" test, install.sh is `#!/usr/bin/env bash\nsleep 0.5\nexit 0`.
    For failure + dirty rollback: install.sh does `echo x > "$ANIMAYA_HUB_DIR/leaked.txt"; exit 1` and manifest.owned_paths = ["leaked.txt"]; uninstall.sh is a no-op (so the leaked file remains).
    For failure + clean rollback: install.sh does `exit 1`; manifest.owned_paths = [].

    Monkeypatch bot.modules.lifecycle.DEFAULT_HUB_DIR and DEFAULT_MODULES_ROOT if needed; but job runner should accept module_dir + hub_dir as args.

    Create tests/dashboard/test_modules.py. Use build_client helper + make_session_cookie. For "seeded modules" tests, put modules under tmp_path/modules and set env ANIMAYA_MODULES_DIR=str(tmp_path/"modules"). App factory should read this env.

    Commit "test(05-05): failing tests for modules list + jobs + install/uninstall UX".
  </action>
  <verify>
    <automated>python -m pytest tests/dashboard/test_jobs.py tests/dashboard/test_modules.py --collect-only -q 2>&1 | tail -5 ; python -m pytest tests/dashboard/test_jobs.py tests/dashboard/test_modules.py -x -q ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - tests/dashboard/test_jobs.py has 8+ tests
    - tests/dashboard/test_modules.py has 9+ tests
    - Collection lists 17+ tests
    - Run exits NON-ZERO (RED)
    - ruff check clean on both
  </acceptance_criteria>
  <done>Red phase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bot/dashboard/jobs.py (GREEN for test_jobs.py)</name>
  <files>bot/dashboard/jobs.py</files>
  <read_first>
    - tests/dashboard/test_jobs.py (contract)
    - .planning/phases/05-web-dashboard/05-RESEARCH.md Pattern 3 (job runner) + Pitfall 2 (subprocess in async) + Pitfall 5 (job GC race)
    - bot/modules/lifecycle.py (install signature + _resolve_owned_path pattern)
    - bot/events.py (emit signature)
    - CLAUDE.md style
  </read_first>
  <behavior>
    Task 1 tests are the contract. Implement enough to pass all 8.
  </behavior>
  <action>
    Create bot/dashboard/jobs.py:

      """In-process async job runner for module install/uninstall (Phase 5 DASH-05).

      Single global asyncio.Lock enforces one-at-a-time semantics (D-08).
      State retained in a dict keyed by uuid4 for 10 minutes after completion
      (D-10). Blocking subprocess work (bot.modules.install / uninstall)
      is dispatched via asyncio.to_thread (Pitfall 2).

      Logs captured by attaching a module-local logging.Handler to the
      "bot.modules" logger for the duration of each job; bounded to last
      200 records, last 50 surfaced in the failure fragment (D-09).
      """
      from __future__ import annotations

      import asyncio
      import logging
      import uuid
      from dataclasses import dataclass, field
      from datetime import datetime, timedelta, timezone
      from pathlib import Path
      from typing import Any

      import bot.modules as bot_modules
      from bot.events import emit as events_emit
      from bot.modules.lifecycle import _resolve_owned_path  # internal helper — acceptable same-phase reuse
      from bot.modules.manifest import validate_manifest

      logger = logging.getLogger(__name__)

      _LOG_CAPACITY = 200
      _TAIL_LINES = 50
      _JOB_RETENTION = timedelta(minutes=10)

      _lock: asyncio.Lock = asyncio.Lock()
      _jobs: dict[str, "JobState"] = {}


      class InProgressError(RuntimeError):
          """Raised when an install/uninstall is attempted while another is running."""


      @dataclass
      class JobState:
          id: str
          op: str                       # "install" | "uninstall"
          module: str
          status: str                   # "running" | "done" | "failed"
          started: datetime
          finished: datetime | None = None
          log_lines: list[str] = field(default_factory=list)
          error: str | None = None
          rollback: str | None = None    # "clean" | "dirty" | None
          leaked_paths: list[str] = field(default_factory=list)


      class _JobLogHandler(logging.Handler):
          def __init__(self, target: list[str]) -> None:
              super().__init__(level=logging.INFO)
              self._target = target
          def emit(self, record: logging.LogRecord) -> None:
              self._target.append(self.format(record))
              if len(self._target) > _LOG_CAPACITY:
                  del self._target[: len(self._target) - _LOG_CAPACITY]


      def _install_handler(buffer: list[str]) -> logging.Handler:
          handler = _JobLogHandler(buffer)
          handler.setFormatter(logging.Formatter("%(asctime)s %(levelname)s %(name)s: %(message)s"))
          logging.getLogger("bot.modules").addHandler(handler)
          return handler


      def _remove_handler(handler: logging.Handler) -> None:
          logging.getLogger("bot.modules").removeHandler(handler)


      def _check_rollback(module_dir: Path, hub_dir: Path) -> tuple[str, list[str]]:
          """Classify rollback state after a failed install."""
          try:
              manifest = validate_manifest(module_dir)
          except Exception:  # noqa: BLE001
              return ("unknown", [])
          leaked: list[str] = []
          for rel in manifest.owned_paths:
              try:
                  if _resolve_owned_path(hub_dir.resolve(), rel).exists():
                      leaked.append(rel)
              except ValueError:
                  continue
          return ("dirty" if leaked else "clean", leaked)


      def _gc() -> None:
          """Evict jobs older than _JOB_RETENTION."""
          now = datetime.now(timezone.utc)
          stale = [jid for jid, j in _jobs.items()
                   if j.finished is not None and (now - j.finished) > _JOB_RETENTION]
          for jid in stale:
              _jobs.pop(jid, None)


      def get_job(job_id: str) -> JobState | None:
          _gc()
          return _jobs.get(job_id)


      async def start_install(
          module_name: str, module_dir: Path, hub_dir: Path,
          *, config: dict | None = None,
      ) -> JobState:
          """Enqueue an install job. Raises InProgressError if another is running."""
          if _lock.locked():
              raise InProgressError("another module operation in progress")
          job = JobState(
              id=uuid.uuid4().hex, op="install", module=module_name,
              status="running", started=datetime.now(timezone.utc),
          )
          _jobs[job.id] = job
          asyncio.create_task(_run_install(job, module_dir, hub_dir, config or {}))
          return job


      async def start_uninstall(
          name: str, hub_dir: Path, module_dir: Path,
      ) -> JobState:
          if _lock.locked():
              raise InProgressError("another module operation in progress")
          job = JobState(
              id=uuid.uuid4().hex, op="uninstall", module=name,
              status="running", started=datetime.now(timezone.utc),
          )
          _jobs[job.id] = job
          asyncio.create_task(_run_uninstall(job, name, hub_dir, module_dir))
          return job


      async def _run_install(
          job: JobState, module_dir: Path, hub_dir: Path, config: dict,
      ) -> None:
          async with _lock:
              handler = _install_handler(job.log_lines)
              events_emit("info", "modules.install", f"install started: {job.module}",
                          job_id=job.id)
              try:
                  await asyncio.to_thread(bot_modules.install, module_dir, hub_dir, config=config)
                  job.status = "done"
                  events_emit("info", "modules.install", f"install done: {job.module}",
                              job_id=job.id)
              except Exception as exc:  # noqa: BLE001
                  job.status = "failed"
                  job.error = f"{type(exc).__name__}: {exc}"
                  job.rollback, job.leaked_paths = _check_rollback(module_dir, hub_dir)
                  events_emit("error", "modules.install",
                              f"install failed: {job.module}",
                              job_id=job.id, error=job.error,
                              rollback=job.rollback, leaked_paths=job.leaked_paths,
                              log=job.log_lines[-_TAIL_LINES:])
              finally:
                  _remove_handler(handler)
                  job.finished = datetime.now(timezone.utc)
                  _gc()


      async def _run_uninstall(
          job: JobState, name: str, hub_dir: Path, module_dir: Path,
      ) -> None:
          async with _lock:
              handler = _install_handler(job.log_lines)
              events_emit("info", "modules.uninstall", f"uninstall started: {name}",
                          job_id=job.id)
              try:
                  await asyncio.to_thread(bot_modules.uninstall, name, hub_dir, module_dir)
                  job.status = "done"
                  events_emit("info", "modules.uninstall", f"uninstall done: {name}",
                              job_id=job.id)
              except Exception as exc:  # noqa: BLE001
                  job.status = "failed"
                  job.error = f"{type(exc).__name__}: {exc}"
                  events_emit("error", "modules.uninstall",
                              f"uninstall failed: {name}",
                              job_id=job.id, error=job.error,
                              log=job.log_lines[-_TAIL_LINES:])
              finally:
                  _remove_handler(handler)
                  job.finished = datetime.now(timezone.utc)
                  _gc()


      # Test hook: exposed for monkeypatching _JOB_RETENTION
      def _set_retention_for_tests(delta: timedelta) -> None:
          global _JOB_RETENTION
          _JOB_RETENTION = delta


      __all__ = [
          "JobState", "InProgressError",
          "start_install", "start_uninstall", "get_job",
          "_set_retention_for_tests",
      ]

    Commit "feat(05-05): asyncio job runner with global lock + rollback classification + events emission".
  </action>
  <verify>
    <automated>python -m pytest tests/dashboard/test_jobs.py -x -q && ruff check bot/dashboard/jobs.py</automated>
  </verify>
  <acceptance_criteria>
    - bot/dashboard/jobs.py exists
    - grep "asyncio.Lock" bot/dashboard/jobs.py succeeds
    - grep "asyncio.to_thread" bot/dashboard/jobs.py succeeds
    - grep "class JobState" bot/dashboard/jobs.py succeeds
    - grep "class InProgressError" bot/dashboard/jobs.py succeeds
    - grep "events_emit" bot/dashboard/jobs.py succeeds (emitter wired)
    - tests/dashboard/test_jobs.py — all 8 tests pass
    - ruff check clean
    - wc -l bot/dashboard/jobs.py >= 130
  </acceptance_criteria>
  <done>Jobs runner green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement modules_view + templates + module_routes (GREEN for test_modules.py)</name>
  <files>bot/dashboard/modules_view.py, bot/dashboard/module_routes.py, bot/dashboard/templates/modules.html, bot/dashboard/templates/module_detail.html, bot/dashboard/templates/_fragments/module_card.html, bot/dashboard/templates/_fragments/module_card_running.html, bot/dashboard/templates/_fragments/module_card_failed.html, bot/dashboard/templates/_fragments/conflict_toast.html</files>
  <read_first>
    - tests/dashboard/test_modules.py (contract)
    - bot/dashboard/jobs.py (import surface)
    - bot/modules/__init__.py + lifecycle.py (install/uninstall signatures, DEFAULT_MODULES_ROOT)
    - bot/modules/manifest.py (ModuleManifest fields)
    - .planning/phases/05-web-dashboard/05-UI-SPEC.md Page Topology /modules + Install/Uninstall Job UX + Concurrency (409)
  </read_first>
  <behavior>
    Task 1 modules tests are the contract.
  </behavior>
  <action>
    File 1 — bot/dashboard/modules_view.py:

      """Module discovery + description helpers (Phase 5 DASH-04).

      Distinguishes 'available' (manifest on disk, not in registry) from
      'installed' (registry entry). Wraps bot.modules primitives so the
      route layer stays thin.
      """
      from __future__ import annotations

      import logging
      import os
      from dataclasses import dataclass
      from pathlib import Path

      from bot.modules import get_entry, read_registry, validate_manifest
      from bot.modules.lifecycle import DEFAULT_MODULES_ROOT

      logger = logging.getLogger(__name__)


      def modules_root() -> Path:
          raw = os.environ.get("ANIMAYA_MODULES_DIR")
          return Path(raw).resolve() if raw else DEFAULT_MODULES_ROOT.resolve()


      @dataclass
      class ModuleCard:
          name: str
          version: str
          description: str
          owned_paths: list[str]
          depends: list[str]
          has_config: bool
          installed: bool
          installed_at: str | None

      def _describe_from_disk(module_dir: Path) -> ModuleCard | None:
          try:
              m = validate_manifest(module_dir)
          except Exception as exc:  # noqa: BLE001
              logger.warning("skip module %s: %s", module_dir.name, exc)
              return None
          return ModuleCard(
              name=m.name, version=m.version, description=m.description,
              owned_paths=list(m.owned_paths), depends=list(m.depends),
              has_config=bool(m.config_schema and m.config_schema.get("properties")),
              installed=False, installed_at=None,
          )


      def all_cards(hub_dir: Path) -> tuple[list[ModuleCard], list[ModuleCard]]:
          """Return (installed, available) — installed first in registry order."""
          root = modules_root()
          installed_names = [e["name"] for e in read_registry(hub_dir)["modules"]]
          installed: list[ModuleCard] = []
          available: list[ModuleCard] = []
          for child in sorted(root.iterdir()) if root.is_dir() else []:
              if not child.is_dir():
                  continue
              card = _describe_from_disk(child)
              if card is None:
                  continue
              if card.name in installed_names:
                  entry = get_entry(hub_dir, card.name)
                  card.installed = True
                  card.installed_at = entry.get("installed_at") if entry else None
                  installed.append(card)
              else:
                  available.append(card)
          return installed, available


      def describe(hub_dir: Path, name: str) -> ModuleCard | None:
          root = modules_root()
          candidate = root / name
          if not candidate.is_dir():
              return None
          card = _describe_from_disk(candidate)
          if card is None:
              return None
          entry = get_entry(hub_dir, name)
          if entry is not None:
              card.installed = True
              card.installed_at = entry.get("installed_at")
          return card


      def module_dir_for(name: str) -> Path:
          return modules_root() / name


      __all__ = ["ModuleCard", "all_cards", "describe", "modules_root", "module_dir_for"]

    File 2 — bot/dashboard/module_routes.py:

      """Module list + detail + install/uninstall + job status routes (Phase 5)."""
      from __future__ import annotations

      import logging
      from pathlib import Path

      from fastapi import Depends, FastAPI, HTTPException, Request
      from fastapi.responses import HTMLResponse
      from fastapi.templating import Jinja2Templates

      from bot.dashboard.deps import require_owner
      from bot.dashboard.jobs import (
          InProgressError, get_job, start_install, start_uninstall,
      )
      from bot.dashboard.modules_view import all_cards, describe, module_dir_for

      logger = logging.getLogger(__name__)


      def register(app: FastAPI, templates: Jinja2Templates) -> None:
          hub_dir: Path = app.state.hub_dir

          @app.get("/modules", response_class=HTMLResponse, name="modules_list")
          async def modules_list(request: Request, _uid: int = Depends(require_owner)):
              installed, available = all_cards(hub_dir)
              return templates.TemplateResponse("modules.html", {
                  "request": request,
                  "installed": installed, "available": available,
                  "nav_active": "modules",
              })

          @app.get("/modules/{name}", response_class=HTMLResponse, name="module_detail")
          async def module_detail(name: str, request: Request,
                                  _uid: int = Depends(require_owner)):
              card = describe(hub_dir, name)
              if card is None:
                  raise HTTPException(status_code=404, detail=f"module {name!r} not found")
              return templates.TemplateResponse("module_detail.html", {
                  "request": request, "card": card, "nav_active": "modules",
              })

          @app.post("/modules/{name}/install", response_class=HTMLResponse)
          async def install_endpoint(name: str, request: Request,
                                     _uid: int = Depends(require_owner)):
              card = describe(hub_dir, name)
              if card is None:
                  raise HTTPException(status_code=404, detail=f"module {name!r} not found")
              try:
                  job = await start_install(name, module_dir_for(name), hub_dir)
              except InProgressError:
                  resp = templates.TemplateResponse(
                      "_fragments/conflict_toast.html",
                      {"request": request, "message": "Another module operation is in progress. Wait for it to finish, then try again."},
                  )
                  resp.status_code = 409
                  return resp
              return templates.TemplateResponse(
                  "_fragments/module_card_running.html",
                  {"request": request, "card": card, "job": job},
              )

          @app.post("/modules/{name}/uninstall", response_class=HTMLResponse)
          async def uninstall_endpoint(name: str, request: Request,
                                       _uid: int = Depends(require_owner)):
              card = describe(hub_dir, name)
              if card is None:
                  raise HTTPException(status_code=404, detail=f"module {name!r} not found")
              try:
                  job = await start_uninstall(name, hub_dir, module_dir_for(name))
              except InProgressError:
                  resp = templates.TemplateResponse(
                      "_fragments/conflict_toast.html",
                      {"request": request, "message": "Another module operation is in progress. Wait for it to finish, then try again."},
                  )
                  resp.status_code = 409
                  return resp
              return templates.TemplateResponse(
                  "_fragments/module_card_running.html",
                  {"request": request, "card": card, "job": job},
              )

          @app.get("/modules/{name}/job/{job_id}", response_class=HTMLResponse)
          async def job_status(name: str, job_id: str, request: Request,
                               _uid: int = Depends(require_owner)):
              job = get_job(job_id)
              if job is None:
                  raise HTTPException(status_code=404, detail="job expired or unknown")
              card = describe(hub_dir, name)  # may be None if uninstall succeeded
              if job.status == "running":
                  return templates.TemplateResponse(
                      "_fragments/module_card_running.html",
                      {"request": request, "card": card, "job": job},
                  )
              if job.status == "done":
                  return templates.TemplateResponse(
                      "_fragments/module_card.html",
                      {"request": request, "card": card, "job_done": True},
                  )
              # failed
              return templates.TemplateResponse(
                  "_fragments/module_card_failed.html",
                  {"request": request, "card": card, "job": job,
                   "log_tail": job.log_lines[-50:]},
              )


      __all__ = ["register"]

    File 3 — bot/dashboard/templates/modules.html:

      {% extends "base.html" %}
      {% block title %}Modules — Animaya{% endblock %}
      {% set nav_active = "modules" %}
      {% block content %}
      <div class="panel">
        <div class="panel-header">Installed</div>
        {% if installed %}
          <div class="modules-grid">
            {% for card in installed %}{% include "_fragments/module_card.html" %}{% endfor %}
          </div>
        {% else %}
          <p class="field-help"><strong>No modules installed</strong><br>
            Browse available modules below and click <strong>Install</strong> to add capabilities.</p>
        {% endif %}
      </div>
      <div class="panel">
        <div class="panel-header">Available</div>
        {% if available %}
          <div class="modules-grid">
            {% for card in available %}{% include "_fragments/module_card.html" %}{% endfor %}
          </div>
        {% else %}
          <p class="field-help"><strong>No modules available</strong><br>
            All modules in <code>modules/*/</code> are installed. Add new module folders to extend.</p>
        {% endif %}
      </div>
      {% endblock %}

    File 4 — bot/dashboard/templates/module_detail.html:

      {% extends "base.html" %}
      {% block title %}{{ card.name }} — Animaya{% endblock %}
      {% set nav_active = "modules" %}
      {% block content %}
      <div class="panel">
        <div class="panel-header">
          {{ card.name }}
          {% if card.installed %}<span class="badge-green">installed</span>{% endif %}
        </div>
        <p><strong>Version</strong>: {{ card.version }}</p>
        <p>{{ card.description }}</p>
        {% if card.owned_paths %}
          <p><strong>Owned paths</strong>:</p>
          <ul>{% for p in card.owned_paths %}<li><code>{{ p }}</code></li>{% endfor %}</ul>
        {% endif %}
        {% if card.has_config and card.installed %}
          <div id="config-form-mount">
            <!-- Plan 06 will populate this via an HTMX swap from /modules/{name}/config -->
            <p class="field-help">Configuration form — added in Plan 05-06.</p>
          </div>
        {% elif not card.installed %}
          <p class="field-help">Install this module to configure it.</p>
        {% else %}
          <p class="field-help">This module has no configuration.</p>
        {% endif %}
        <p style="margin-top:1.5rem">
          {% if card.installed %}
            <button class="btn btn-danger"
                    hx-post="/modules/{{ card.name }}/uninstall"
                    hx-confirm="Uninstall {{ card.name }}? This will remove all files under its owned_paths and rebuild CLAUDE.md."
                    hx-target="#status-toast"
                    hx-swap="innerHTML">Uninstall</button>
          {% else %}
            <button class="btn btn-success"
                    hx-post="/modules/{{ card.name }}/install"
                    hx-target="#status-toast"
                    hx-swap="innerHTML">Install</button>
          {% endif %}
          <a class="btn" href="/modules">Back to modules</a>
        </p>
      </div>
      {% endblock %}

    File 5 — bot/dashboard/templates/_fragments/module_card.html:

      <div class="card">
        <h3 style="margin:0 0 .5rem;font-size:1rem;text-transform:uppercase;letter-spacing:.05em;color:var(--text-dim)">{{ card.name }}</h3>
        {% if card.installed %}<span class="badge-green">installed</span>{% endif %}
        <span class="event-ts">v{{ card.version }}</span>
        <p style="margin:.5rem 0">{{ card.description }}</p>
        <div style="display:flex;gap:.5rem">
          <a class="btn" href="/modules/{{ card.name }}">
            {% if card.installed %}Configure{% else %}Details{% endif %}
          </a>
          {% if card.installed %}
            <button class="btn btn-danger"
                    hx-post="/modules/{{ card.name }}/uninstall"
                    hx-confirm="Uninstall {{ card.name }}? This will remove all files under its owned_paths and rebuild CLAUDE.md."
                    hx-target="closest .card"
                    hx-swap="outerHTML">Uninstall</button>
          {% else %}
            <button class="btn btn-success"
                    hx-post="/modules/{{ card.name }}/install"
                    hx-target="closest .card"
                    hx-swap="outerHTML">Install</button>
          {% endif %}
        </div>
      </div>

    File 6 — bot/dashboard/templates/_fragments/module_card_running.html:

      <div class="card job-running"
           hx-get="/modules/{{ card.name if card else job.module }}/job/{{ job.id }}"
           hx-trigger="every 1s"
           hx-swap="outerHTML"
           aria-live="polite">
        <h3 style="margin:0 0 .5rem">{{ card.name if card else job.module }}</h3>
        <span class="badge pending">{{ "Installing" if job.op == "install" else "Uninstalling" }}…</span>
        <div class="progress"><div class="progress-bar progress-indeterminate"></div></div>
      </div>

    File 7 — bot/dashboard/templates/_fragments/module_card_failed.html:

      <div class="card job-failed">
        <h3 style="margin:0 0 .5rem">{{ job.module }}</h3>
        <div class="error">
          {{ "Install" if job.op == "install" else "Uninstall" }} failed: {{ job.module }}
          {% if job.rollback %}
            {% if job.rollback == "clean" %}
              <span class="badge-green" style="margin-left:.5rem">rollback: clean</span>
            {% elif job.rollback == "dirty" %}
              <span class="badge-red" style="margin-left:.5rem">rollback: dirty — {{ job.leaked_paths | length }} paths leaked</span>
            {% endif %}
          {% endif %}
        </div>
        {% if log_tail %}
          <details>
            <summary>Show last {{ log_tail | length }} log lines</summary>
            <pre class="job-log">{% for line in log_tail %}{{ line }}
      {% endfor %}</pre>
          </details>
        {% endif %}
        {% if card %}
          <button class="btn"
                  hx-post="/modules/{{ card.name }}/{{ job.op }}"
                  hx-target="closest .card"
                  hx-swap="outerHTML">Retry</button>
        {% endif %}
      </div>

    File 8 — bot/dashboard/templates/_fragments/conflict_toast.html:

      <div class="error" id="status-toast">{{ message }}</div>

    Commit "feat(05-05): /modules browse + install/uninstall job UX with 1s HTMX polling".
  </action>
  <verify>
    <automated>python -m pytest tests/dashboard/test_modules.py tests/dashboard/test_jobs.py -x -q && python -m pytest tests/dashboard/ -x -q && ruff check bot/dashboard/modules_view.py bot/dashboard/module_routes.py</automated>
  </verify>
  <acceptance_criteria>
    - bot/dashboard/modules_view.py exists with all_cards + describe + module_dir_for
    - bot/dashboard/module_routes.py exists with register(app, templates)
    - Five template files in place (modules.html, module_detail.html, three card fragments + conflict toast)
    - /modules, /modules/{name}, POST /modules/{name}/install, POST /modules/{name}/uninstall, GET /modules/{name}/job/{job_id} all registered (grep name= attributes in routes file)
    - grep "Depends(require_owner)" bot/dashboard/module_routes.py shows at least 5 matches (every route guarded)
    - grep "hx-trigger" bot/dashboard/templates/_fragments/module_card_running.html contains "every 1s"
    - grep "409" bot/dashboard/module_routes.py succeeds (conflict status code set)
    - tests/dashboard/test_modules.py + test_jobs.py — all 17 tests pass
    - Full dashboard suite python -m pytest tests/dashboard/ -x -q exits 0
    - Whole suite python -m pytest tests/ -v exits 0
    - ruff check bot/dashboard/ clean
  </acceptance_criteria>
  <done>/modules browse + install/uninstall UX complete and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /modules/{name}/install,uninstall | authenticated owner triggers privileged script execution |
| bot.modules.install -> bash subprocess (install.sh) | runs with bot process UID against hub filesystem |
| jobs dict -> memory | bounded 10-min retention |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | E (Elevation) | install/uninstall without auth | mitigate | Every route Depends(require_owner). Tests test_modules_list_requires_owner + (extend to POST variants if missing). |
| T-05-05-02 | S (Command injection) | module name in URL -> shell | mitigate | bot.modules.lifecycle._validate_name rejects any name not matching `^[a-z][a-z0-9_-]*$` at install time. Route passes name through unchanged; subprocess uses list-form. |
| T-05-05-03 | T (Manifest poisoning) | malicious manifest with owned_paths outside hub | mitigate | bot.modules.lifecycle._validate_owned_paths (already enforced in Phase 3) rejects absolute paths and ".." — reused via normal install path. |
| T-05-05-04 | D (Concurrent install DoS) | flood POSTs to exhaust threads | mitigate | Single global asyncio.Lock; second request returns 409 immediately without spawning a task. Test test_second_install_returns_409_conflict_toast. |
| T-05-05-05 | D (Event loop starvation) | subprocess blocks async loop | mitigate | asyncio.to_thread wraps install/uninstall. Test test_install_does_not_block_event_loop proves concurrent sleeps complete. |
| T-05-05-06 | I (Info Disclosure) | job log leaks secrets on failure | mitigate | Handler buffers records from "bot.modules" logger only; Phase 3 logger never logs tokens. Log tail is owner-only fragment. |
| T-05-05-07 | T (CSRF) | cross-site POST triggers install | mitigate | SameSite=Lax session cookie (Plan 02/03) + Login Widget HMAC for auth; unauthenticated cross-site POST returns 302 to /login. |
| T-05-05-08 | R (Replay) | same job_id queried after eviction | mitigate | GET /modules/{name}/job/{id} -> 404 with friendly HTML when get_job returns None; RESEARCH Pitfall 5 addressed via events.log emitting failure details so long-term audit survives GC. |
| T-05-05-09 | T (Template injection) | user-controlled manifest description rendered | mitigate | Jinja2 autoescape on; no `| safe` filter in module templates. |
| T-05-05-10 | T (Destructive uninstall via prefetch) | browser prefetches DELETE-like URLs | mitigate | Uninstall is POST not GET; uninstall button includes hx-confirm requiring user gesture. |
</threat_model>

<verification>
python -m pytest tests/dashboard/ -v — all tests pass (events, auth, app, login, status, home, jobs, modules). Whole suite python -m pytest tests/ -v green. ruff check bot/dashboard/ clean.
</verification>

<success_criteria>
- /modules page lists installed + available with correct classification
- /modules/{name} renders metadata + owned_paths + depends
- Install/uninstall endpoints enqueue async job, return running fragment with 1s polling
- Job endpoint returns done/failed fragment with log tail + rollback badge
- Concurrent request returns 409 with conflict toast
- Jobs auto-evicted 10 min after finish
- 17+ tests all green
</success_criteria>

<output>
After completion, create .planning/phases/05-web-dashboard/05-05-SUMMARY.md summarising jobs contract, URL surface, rollback classification rules, and event emitter integration.
</output>
