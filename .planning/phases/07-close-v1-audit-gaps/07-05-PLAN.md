---
phase: 07-close-v1-audit-gaps
plan: 05
type: execute
wave: 3
depends_on: [07-01, 07-02, 07-03, 07-04]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - INST-01
  - INST-02
  - INST-03
  - INST-04
  - TELE-01
  - TELE-02
  - TELE-03
  - TELE-04
  - TELE-05
  - MODS-01
  - MODS-02
  - MODS-03
  - MODS-04
  - MODS-05
  - MODS-06
  - IDEN-01
  - IDEN-02
  - IDEN-03
  - IDEN-04
  - MEMO-01
  - MEMO-02
  - MEMO-03
  - MEMO-04
  - GITV-01
  - GITV-02
  - GITV-03
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - TEST-01
  - TEST-02
  - TEST-03
gap_closure: true
tags: [bookkeeping, requirements, traceability]

must_haves:
  truths:
    - "Every REQ-ID checkbox in REQUIREMENTS.md reflects shipped reality per VERIFICATION.md results"
    - "Traceability table has an entry for every requirement with true phase + status"
    - "TEST-01, TEST-02, TEST-03 rows exist in the traceability table (Phase 6 harness)"
    - "No [ ] checkbox remains next to a requirement that shipped as SATISFIED per its phase VERIFICATION.md"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Authoritative checkbox + traceability state for milestone v1.0"
      contains: "TEST-01"
    - path: ".planning/REQUIREMENTS.md"
      contains: "TEST-03"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/{phase}/*-VERIFICATION.md"
      via: "status column in traceability table matches VERIFICATION.md verdict"
      pattern: "Complete|Pending|Unsatisfied"
---

<objective>
Reconcile `.planning/REQUIREMENTS.md` with shipped reality. Update checkboxes and the traceability table so every REQ-ID matches the verdict produced by the VERIFICATION.md artifacts created in waves 1–2. Insert TEST-01/02/03 rows for the Phase 6 telethon harness (currently missing from the traceability table).

Purpose: Audit flagged REQUIREMENTS.md as stale: checkboxes for INST-*, MODS-*, IDEN-*, MEMO-*, GITV-*, DASH-* remain `[ ]` despite phases being complete; traceability table shows `Pending` for all but TELE-*; TEST-* not listed at all.

Output: One file modified — `.planning/REQUIREMENTS.md` — with all checkboxes and traceability rows reflecting the truth from VERIFICATION.md artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/07-close-v1-audit-gaps/07-CONTEXT.md

# Target file (read current state before editing)
@.planning/REQUIREMENTS.md

# Source-of-truth verification artifacts (read all, including those produced by this phase's prior waves)
@.planning/phases/01-install-foundation/01-VERIFICATION.md
@.planning/phases/02-telegram-bridge/02-VERIFICATION.md
@.planning/phases/03-module-system/03-VERIFICATION.md
@.planning/phases/04-first-party-modules/04-VERIFICATION.md
@.planning/phases/05-web-dashboard/05-VERIFICATION.md
@.planning/phases/06-telethon-test-harness-at-hub-level-for-end-to-end-telegram-b/06-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile REQUIREMENTS.md checkboxes with VERIFICATION.md verdicts</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — identify every REQ-ID with a `[ ]` or `[x]` checkbox and its exact line)
    - .planning/v1.0-MILESTONE-AUDIT.md (tech_debt section: "REQUIREMENTS.md checkboxes for INST-*, MODS-*, IDEN-*, MEMO-*, GITV-*, DASH-* remain [ ] despite completed phases")
    - .planning/phases/01-install-foundation/01-VERIFICATION.md (INST-01..04 verdicts)
    - .planning/phases/02-telegram-bridge/02-VERIFICATION.md (TELE-01..05 verdicts — produced by 07-01)
    - .planning/phases/03-module-system/03-VERIFICATION.md (MODS-01..06 verdicts — produced by 07-02)
    - .planning/phases/04-first-party-modules/04-VERIFICATION.md (IDEN/MEMO/GITV verdicts)
    - .planning/phases/05-web-dashboard/05-VERIFICATION.md (DASH-01..06 verdicts — produced by 07-03)
    - .planning/phases/06-telethon-test-harness-at-hub-level-for-end-to-end-telegram-b/06-VERIFICATION.md (TEST-01..03 verdicts)
  </read_first>
  <action>
Edit `.planning/REQUIREMENTS.md` in a single pass. Use the Edit tool for each discrete change rather than rewriting the whole file.

**Step 1 — Build verdict map** from the 6 VERIFICATION.md files (read-only), mapping every REQ-ID to one of: SATISFIED, UNSATISFIED, PARTIAL.

**Step 2 — Update checkboxes.** For each REQ-ID with a `[ ]` or `[x]` in REQUIREMENTS.md:
- If verdict is SATISFIED: ensure checkbox is `[x]`
- If verdict is UNSATISFIED: ensure checkbox is `[ ]` (leave unchecked) and append inline comment: `<!-- unsatisfied: see phase-NN-VERIFICATION.md -->`
- If verdict is PARTIAL: keep `[ ]` and inline comment: `<!-- partial: see phase-NN-VERIFICATION.md -->`

**Step 3 — Update traceability table.** Locate the traceability table (currently shows `Pending` for most rows, `Complete` for TELE-*). For each row:
- Phase column: set to the phase that shipped the requirement (INST→01, TELE→02, MODS→03, IDEN/MEMO/GITV→04, DASH→05, TEST→06)
- Status column: `Complete` if SATISFIED, `Unsatisfied` if UNSATISFIED, `Partial` if PARTIAL
- If TEST-01/TEST-02/TEST-03 rows are missing, INSERT them at the bottom of the table (or in the section matching table grouping) with phase=06 and status per 06-VERIFICATION.md

**Step 4 — Idempotency.** Ensure repeated runs of this task produce no diff. If a row already matches verdict, leave untouched.

**Anti-patterns to avoid:**
- Do NOT rewrite the whole file in one Write call — use targeted Edit ops
- Do NOT change the format/shape of the traceability table beyond the cells named above
- Do NOT invent verdicts — if a VERIFICATION.md does not exist for a phase, that's an upstream-wave failure; stop and report

Grep-verifiable output targets:
- `grep -c "^- \[x\] INST-" REQUIREMENTS.md` returns 4
- `grep -c "^- \[x\] IDEN-" REQUIREMENTS.md` returns 4
- `grep -c "^- \[x\] MEMO-" REQUIREMENTS.md` returns 4
- `grep -c "^- \[x\] GITV-" REQUIREMENTS.md` returns 3
- `grep "TEST-01" REQUIREMENTS.md` returns a line
- `grep "TEST-02" REQUIREMENTS.md` returns a line
- `grep "TEST-03" REQUIREMENTS.md` returns a line
- `grep -c "Pending" REQUIREMENTS.md` should shrink significantly (only remaining Pending entries are legitimately pending / future-milestone)
  </action>
  <verify>
    <automated>
# Confirm TEST-* rows exist and satisfied requirements are checked
fail=0; \
for id in TEST-01 TEST-02 TEST-03; do \
  grep -q "$id" .planning/REQUIREMENTS.md || { echo "missing $id row"; fail=1; }; \
done; \
# INST-01..04 must be checked (proven satisfied by 01-VERIFICATION.md)
checked_inst=$(grep -cE "^- \[x\] (INST-0[1-4])" .planning/REQUIREMENTS.md || true); \
[ "$checked_inst" -ge 4 ] || { echo "INST-01..04 not all checked (got $checked_inst/4)"; fail=1; }; \
# IDEN/MEMO/GITV similarly (satisfied by 04-VERIFICATION.md 11/11)
checked_iden=$(grep -cE "^- \[x\] (IDEN-0[1-4]|MEMO-0[1-4]|GITV-0[1-3])" .planning/REQUIREMENTS.md || true); \
[ "$checked_iden" -ge 11 ] || { echo "IDEN/MEMO/GITV not all checked (got $checked_iden/11)"; fail=1; }; \
exit $fail
    </automated>
  </verify>
  <done>
REQUIREMENTS.md checkboxes match VERIFICATION.md verdicts for all 27+3 REQ-IDs. Traceability table has phase + status per REQ-ID. TEST-01/02/03 rows present.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human re-runs `/gsd-audit-milestone 1.0` and confirms passed status</name>
  <what-built>
All four bookkeeping artifacts from Phase 07 waves 1–3:
- Three new VERIFICATION.md files (02, 03, 05)
- Two new VALIDATION.md files (02, 06)
- Nyquist sign-off frontmatter on 24 SUMMARY.md files
- Reconciled REQUIREMENTS.md checkboxes + traceability table
  </what-built>
  <how-to-verify>
1. Run `/gsd-audit-milestone 1.0`
2. Confirm output contains `status: passed`
3. Confirm `phase_verifications_missing` is empty (or absent)
4. Confirm `nyquist.overall` is `compliant` (or `partial` with only the expected Phase 07 self-validation gap)
5. Confirm `requirements` section shows no `status: unsatisfied` rows without evidence
6. If status is NOT passed, capture the exact gaps reported and feed them back to planner for a new gap-closure phase
  </how-to-verify>
  <resume-signal>Type "passed" to proceed to milestone archival, or paste the gap list from the audit for follow-up planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Bookkeeping edits to markdown only; no attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | T (Tampering) | REQUIREMENTS.md checkbox flips | mitigate | Edits are idempotent + grep-verifiable; human checkpoint re-runs audit to confirm integrity. |
</threat_model>

<verification>
- REQUIREMENTS.md checkboxes match VERIFICATION.md verdicts for every REQ-ID
- Traceability table shows true phase + status per REQ-ID
- TEST-01/02/03 rows present
- `/gsd-audit-milestone 1.0` yields `status: passed` (confirmed at Task 2 checkpoint)
</verification>

<success_criteria>
Milestone v1.0 can be archived: `/gsd-audit-milestone 1.0` yields `status: passed` with no `phase_verifications_missing`, no `unsatisfied` rows without evidence, and Nyquist overall `compliant` or `partial` with only acknowledged gaps.
</success_criteria>

<output>
After completion, create `.planning/phases/07-close-v1-audit-gaps/07-05-SUMMARY.md`.
</output>
