---
phase: 14
plan: 04
type: execute
wave: 3
depends_on: [14-03]
files_modified:
  - dashboard/app/api/integration/v1/meta/route.ts
  - dashboard/DASHBOARD.md
autonomous: true
requirements: [REQ-SPEC-07, REQ-SPEC-08]
tags: [voidnet, meta-endpoint, docs]

must_haves:
  truths:
    - "GET /api/integration/v1/meta with valid voidnet signature → 200 JSON {version, supported_auth_modes:['telegram','voidnet'], dashboard_port:8090}"
    - "GET /api/integration/v1/meta without valid signature → 401 (middleware enforces, handler is gated)"
    - "`dashboard/DASHBOARD.md` documents both VOIDNET_HMAC_SECRET and its OWNER_TELEGRAM_ID pairing invariant"
    - "All 7 Plan 01 E2E voidnet tests GREEN"
  artifacts:
    - path: "dashboard/app/api/integration/v1/meta/route.ts"
      provides: "GET handler returning dashboard version + capabilities"
      exports: ["GET"]
    - path: "dashboard/DASHBOARD.md"
      provides: "Documented env contract for voidnet integration"
      contains: "VOIDNET_HMAC_SECRET"
  key_links:
    - from: "dashboard/app/api/integration/v1/meta/route.ts"
      to: "dashboard/middleware.ts (voidnet branch)"
      via: "middleware matcher /((?!_next/static|_next/image|favicon.ico).*) covers /api/*"
      pattern: "supported_auth_modes"
    - from: "dashboard/DASHBOARD.md"
      to: "env contract"
      via: "sections for VOIDNET_HMAC_SECRET and OWNER_TELEGRAM_ID"
      pattern: "VOIDNET_HMAC_SECRET"
---

<objective>
Ship the `/api/integration/v1/meta` probe endpoint (REQ-SPEC-07) and document the voidnet env contract in `dashboard/DASHBOARD.md` (REQ-SPEC-08). The endpoint relies entirely on middleware for auth — it only reads package.json version + env. The doc update satisfies the grep acceptance test from SPEC.md.

Purpose: Completes REQ-SPEC-07 and REQ-SPEC-08; closes remaining 2/7 E2E voidnet tests.
Output: New route handler + doc update. All 7 Plan 01 voidnet E2E tests GREEN. All 11 SPEC acceptance criteria satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-voidnet-integration-auth/14-SPEC.md
@.planning/phases/14-voidnet-integration-auth/14-RESEARCH.md (Meta endpoint section)
@.planning/phases/14-voidnet-integration-auth/14-PATTERNS.md (meta route section)
@dashboard/app/api/hub/tree/route.ts
@dashboard/tests/e2e/voidnet.spec.ts

<interfaces>
<!-- Locked response shape -->

Response (200):
  {
    "version": "<package.json version>",
    "supported_auth_modes": ["telegram", "voidnet"],
    "dashboard_port": 8090
  }

Error (401): middleware-emitted when signature absent or invalid. Handler itself does NOT gate — matcher covers /api/*, middleware voidnet block catches the request first.

Runtime: match existing `app/api/hub/tree/route.ts` — `export const runtime = "nodejs";` is the project convention unless Edge is required. Either works since middleware gates first.

NOTE: when the request has no voidnet signature AND no AUTH_SECRET session cookie AND no DASHBOARD_TOKEN, the existing Telegram session gate at the end of middleware will redirect to /login (302). The Plan-01 test "GET /api/integration/v1/meta without sig → 401" was written expecting a JSON 401. To reconcile: when the request targets `/api/integration/v1/*` specifically, the Telegram session gate should return JSON 401 rather than a 302 /login redirect (API route semantics). Task 2 addresses this.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create /api/integration/v1/meta route handler</name>
  <files>dashboard/app/api/integration/v1/meta/route.ts</files>
  <read_first>
    - dashboard/app/api/hub/tree/route.ts (route handler analog — imports, runtime, error shape)
    - dashboard/package.json (read version field at runtime)
    - dashboard/tests/e2e/voidnet.spec.ts (meta test expectations)
  </read_first>
  <behavior>
    - `GET /api/integration/v1/meta` with middleware-passed voidnet session → 200 JSON exactly `{version, supported_auth_modes:["telegram","voidnet"], dashboard_port:8090}`.
    - If middleware rejected (401/403), handler never runs.
  </behavior>
  <action>
    Create `dashboard/app/api/integration/v1/meta/route.ts`:

      import "server-only";
      import { NextResponse } from "next/server";
      import pkg from "@/package.json" with { type: "json" };

      export const runtime = "nodejs";

      export async function GET() {
        return NextResponse.json({
          version: pkg.version,
          supported_auth_modes: ["telegram", "voidnet"],
          dashboard_port: Number(process.env.DASHBOARD_PORT ?? 8090),
        });
      }

    If the json-import assertion syntax (`with { type: "json" }`) is not supported by the current tsconfig/bundler, fall back to reading version via:

      import { readFileSync } from "node:fs";
      import { join } from "node:path";
      const pkg = JSON.parse(readFileSync(join(process.cwd(), "package.json"), "utf-8"));

    But only use the fallback if the import-with syntax errors during build. Do NOT add both.

    Do NOT call `auth()` or re-validate headers — middleware is the single gate. This is deliberate and matches RESEARCH.md Meta endpoint guidance: "middleware enforces auth; handler only reads env + returns JSON."
  </action>
  <verify>
    <automated>cd dashboard && bunx playwright test tests/e2e/voidnet.spec.ts -g "meta with valid"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f dashboard/app/api/integration/v1/meta/route.ts`
    - `grep -q 'supported_auth_modes' dashboard/app/api/integration/v1/meta/route.ts`
    - `grep -q '"telegram"' dashboard/app/api/integration/v1/meta/route.ts`
    - `grep -q '"voidnet"' dashboard/app/api/integration/v1/meta/route.ts`
    - `grep -q '8090' dashboard/app/api/integration/v1/meta/route.ts`
    - `grep -q 'export async function GET' dashboard/app/api/integration/v1/meta/route.ts`
    - E2E "GET /api/integration/v1/meta with valid sig → 200 shape" GREEN
  </acceptance_criteria>
  <done>Meta route returns locked shape under valid voidnet session. No handler-side auth logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ensure /api/integration/v1/* returns JSON 401 (not /login redirect) when unauth'd + document env contract</name>
  <files>dashboard/middleware.ts, dashboard/DASHBOARD.md</files>
  <read_first>
    - dashboard/middleware.ts (current Telegram session gate at ~line 136 — the /login redirect block)
    - dashboard/DASHBOARD.md (existing env documentation style — read to find the right insertion section)
    - dashboard/tests/e2e/voidnet.spec.ts (meta test "without sig → 401")
  </read_first>
  <behavior>
    - `GET /api/integration/v1/meta` without voidnet headers AND without a Telegram session → 401 JSON `{error, code}` (not a 302 to /login).
    - All other Telegram gate behavior unchanged — pages still redirect to /login when unauth'd.
    - `dashboard/DASHBOARD.md` contains sections documenting `VOIDNET_HMAC_SECRET` and the `OWNER_TELEGRAM_ID` pairing invariant such that both `grep -q VOIDNET_HMAC_SECRET dashboard/DASHBOARD.md` and `grep -q OWNER_TELEGRAM_ID dashboard/DASHBOARD.md` succeed and the docs explain the pairing.
  </behavior>
  <action>
    Part A — middleware.ts Telegram-gate tweak for /api/integration/v1/*:

    In `dashboard/middleware.ts`, find the Telegram session gate block (currently around line 136 — the `if (!token) { ... url.pathname = "/login"; return applySecurityHeaders(NextResponse.redirect(url), nonce); }` block).

    Modify ONLY the `if (!token)` branch to distinguish integration API paths:

      if (!token) {
        if (pathname.startsWith("/api/integration/v1/")) {
          return applySecurityHeaders(
            NextResponse.json(
              { error: "unauthorized", code: "NO_SESSION" },
              { status: 401 },
            ),
            nonce,
          );
        }
        const url = req.nextUrl.clone();
        url.pathname = "/login";
        return applySecurityHeaders(NextResponse.redirect(url), nonce);
      }

    Rationale: `/api/integration/v1/*` is machine-only and must emit JSON 401 rather than a browser redirect so voidnet-api can detect misconfiguration cleanly. This is a minimal scoped change — no other code paths affected.

    Also apply the same `/api/integration/v1/` JSON-401 branch to the owner gate (the subsequent `if (!isOwnerTelegramIdEdge(telegramId))` block that currently redirects to /403) — when the authed telegramId is not the owner on an integration path, emit `{error: "forbidden", code: "NOT_OWNER"}` status 403 JSON instead of redirecting to /403.

    Part B — DASHBOARD.md env contract doc:

    If `dashboard/DASHBOARD.md` does not exist, create it with a header and an Environment section. Otherwise locate the existing env documentation section (where DASHBOARD_TOKEN / OWNER_TELEGRAM_ID / AUTH_SECRET are already listed, if they are) and add:

      ## Environment: voidnet integration

      ### `VOIDNET_HMAC_SECRET`
      Shared secret between `voidnet-api` and this dashboard that activates voidnet-proxied auth mode. When set, the dashboard accepts HMAC-SHA256-signed `X-Voidnet-*` headers injected by `voidnet-api` and synthesizes a NextAuth session without presenting the Telegram Login Widget. The secret must match the one held by `voidnet-api` for the target LXC. Unset or empty → voidnet mode is disabled and the standalone Telegram login flow runs unchanged (backward compatible).

      ### `OWNER_TELEGRAM_ID` pairing invariant
      `VOIDNET_HMAC_SECRET` always pairs with `OWNER_TELEGRAM_ID`. Every signed voidnet request carries an `X-Voidnet-Telegram-Id` header that MUST equal `OWNER_TELEGRAM_ID`; requests with a mismatching telegram id are rejected 403 with code `VOIDNET_OWNER_MISMATCH` — no silent fallback to the Telegram flow. `voidnet-api` writes `OWNER_TELEGRAM_ID` at LXC provision time so the pairing normally holds; the dashboard still defends the invariant.

      ### Signed request contract
      - Headers: `X-Voidnet-User-Id`, `X-Voidnet-Handle` (3–32 chars, `^[a-z][a-z0-9-]+$`), `X-Voidnet-Telegram-Id`, `X-Voidnet-Timestamp` (unix seconds), `X-Voidnet-Signature` (hex HMAC-SHA256).
      - Signature: `hmac_sha256(VOIDNET_HMAC_SECRET, "{user_id}|{handle}|{telegram_id}|{timestamp}")`
      - Replay window: ±60 seconds.
      - Error codes: `VOIDNET_SCHEMA`, `VOIDNET_STALE`, `VOIDNET_SIG_INVALID` (401) / `VOIDNET_OWNER_MISMATCH` (403).

      See also `/api/integration/v1/meta` for version/capabilities probe.

    If there is no existing DASHBOARD.md, create one with the above content preceded by a top-level `# Dashboard` header.
  </action>
  <verify>
    <automated>cd dashboard && grep -q VOIDNET_HMAC_SECRET DASHBOARD.md && grep -q OWNER_TELEGRAM_ID DASHBOARD.md && bunx playwright test tests/e2e/voidnet.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q VOIDNET_HMAC_SECRET dashboard/DASHBOARD.md`
    - `grep -q OWNER_TELEGRAM_ID dashboard/DASHBOARD.md`
    - `grep -q 'VOIDNET_OWNER_MISMATCH\|pairing' dashboard/DASHBOARD.md` (pairing invariant documented)
    - `grep -q '/api/integration/v1/' dashboard/middleware.ts` (scoped JSON 401 branch added)
    - `grep -q 'NO_SESSION\|"code": "NO_SESSION"' dashboard/middleware.ts`
    - `cd dashboard && bunx playwright test tests/e2e/voidnet.spec.ts` ALL 7 tests GREEN
    - `cd dashboard && bunx playwright test tests/e2e/auth.spec.ts tests/e2e/dashboard-token.spec.ts` still GREEN (no regression — non-integration paths still redirect to /login when unauth'd)
    - `cd dashboard && bun test` GREEN
    - `cd dashboard && bunx playwright test` full suite GREEN
  </acceptance_criteria>
  <done>All 7 Plan 01 voidnet E2E tests GREEN, all 11 SPEC acceptance criteria satisfied, doc grep succeeds, no regressions in Telegram / DASHBOARD_TOKEN / other tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external prober (voidnet-api) → /api/integration/v1/meta | Meta endpoint reveals dashboard version + supported auth modes. Allowed per CONTEXT (voidnet-api caches this for version-compat check) |
| doc sink (DASHBOARD.md) | Publicly committed; must describe env contract without leaking any real secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Info Disclosure (error leak) | meta + integration 401 branch | mitigate | Fixed `{error, code}` JSON shape; JSON 401 for /api/integration/v1/* (Task 2) returns only `code: "NO_SESSION"` or `"NOT_OWNER"` — no header/env echo |
| T-14-09 | Info Disclosure (version + capabilities enumeration) | meta endpoint | accept | CONTEXT explicitly requires voidnet-api to read version via this endpoint; middleware gates access behind valid HMAC — unauthenticated probers receive 401, not the payload |
| T-14-10 | Spoofing (meta endpoint reached without voidnet gate) | middleware matcher + ordering | mitigate | Next.js matcher `/((?!_next/static|_next/image|favicon.ico).*)` covers `/api/*`; Plan 03 voidnet branch precedes public-paths check; Playwright meta-without-sig test locks 401 behavior |
| T-14-11 | Tampering (silent spec drift in docs) | DASHBOARD.md env section | mitigate | Grep-verifiable acceptance ensures `VOIDNET_HMAC_SECRET` + `OWNER_TELEGRAM_ID` sections exist with pairing language |
</threat_model>

<verification>
- `cd dashboard && bun test && bunx playwright test` — full suite GREEN
- `grep -q VOIDNET_HMAC_SECRET dashboard/DASHBOARD.md && grep -q OWNER_TELEGRAM_ID dashboard/DASHBOARD.md`
- All 11 SPEC acceptance criteria (lines 88-98 of 14-SPEC.md) satisfied
</verification>

<success_criteria>
REQ-SPEC-07, REQ-SPEC-08 satisfied. Phase 14 closes with 7/7 voidnet E2E tests GREEN, all existing tests still GREEN, and the env contract documented. Phase ready for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/14-voidnet-integration-auth/14-04-SUMMARY.md` with: meta route path, final E2E pass matrix (all 7 GREEN expected), full-suite result, any JSON-import syntax fallbacks used, confirmation of doc grep matches. Also update `14-VALIDATION.md` per-task map with Task IDs 14-01-1..3, 14-02-1..2, 14-03-1, 14-04-1..2.
</output>
