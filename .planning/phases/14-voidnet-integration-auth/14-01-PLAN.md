---
phase: 14
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - dashboard/lib/voidnet-auth.server.test.ts
  - dashboard/lib/voidnet-jwt-roundtrip.test.ts
  - dashboard/tests/e2e/voidnet.spec.ts
  - dashboard/tests/e2e/_voidnet-helpers.ts
  - dashboard/playwright.config.ts
autonomous: true
requirements: [REQ-SPEC-01, REQ-SPEC-02, REQ-SPEC-03, REQ-SPEC-04, REQ-SPEC-05, REQ-SPEC-06, REQ-SPEC-07]
tags: [voidnet, auth, middleware, edge, hmac, test-fixtures]

must_haves:
  truths:
    - "Unit test file for verifier exists and runs (RED, expected to fail until Plan 02 lands)"
    - "JWT roundtrip smoke test asserts next-auth/jwt encode/decode interop with AUTH_SECRET + cookie-name salt"
    - "E2E spec file and helper exist covering valid/tampered/stale/owner-mismatch/no-secret/meta cases"
    - "Playwright webServer env injects VOIDNET_HMAC_SECRET=test-voidnet-secret"
  artifacts:
    - path: "dashboard/lib/voidnet-auth.server.test.ts"
      provides: "Unit test scaffold with signer helper + hand-computed vector"
      contains: "verifyVoidnetHeaders"
    - path: "dashboard/lib/voidnet-jwt-roundtrip.test.ts"
      provides: "encode()+getToken() salt interop smoke"
      contains: "next-auth/jwt"
    - path: "dashboard/tests/e2e/voidnet.spec.ts"
      provides: "Playwright cases for REQ-3, REQ-5, REQ-6, REQ-7"
      contains: "voidnetHeaders"
    - path: "dashboard/tests/e2e/_voidnet-helpers.ts"
      provides: "Test-side HMAC signer (node:crypto permitted in test runner)"
      exports: ["voidnetHeaders"]
  key_links:
    - from: "dashboard/tests/e2e/voidnet.spec.ts"
      to: "dashboard/tests/e2e/_voidnet-helpers.ts"
      via: "import voidnetHeaders"
      pattern: "from.*_voidnet-helpers"
    - from: "dashboard/playwright.config.ts"
      to: "webServer.env"
      via: "VOIDNET_HMAC_SECRET + OWNER_TELEGRAM_ID"
      pattern: "VOIDNET_HMAC_SECRET"
---

<objective>
Establish Wave 0 validation contract: create failing unit tests for `verifyVoidnetHeaders`, a JWT mint-and-decode smoke test locking the `next-auth/jwt` `encode()` + `getToken()` salt interop, and a Playwright E2E spec with a reusable signer helper. Also inject `VOIDNET_HMAC_SECRET` + `OWNER_TELEGRAM_ID` into Playwright's webServer env so subsequent wave tests run against a middleware instance that sees the secret.

Purpose: Nyquist compliance — every later task has an automated verify that already exists. The hand-computed HMAC vector locks the canonical signature input string `{userId}|{handle}|{telegramId}|{timestamp}` into CI so Plan 02's verifier cannot drift.
Output: Four test files + updated playwright.config.ts. Unit tests fail RED (expected — verifier not implemented yet).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-voidnet-integration-auth/14-SPEC.md
@.planning/phases/14-voidnet-integration-auth/14-CONTEXT.md
@.planning/phases/14-voidnet-integration-auth/14-RESEARCH.md
@.planning/phases/14-voidnet-integration-auth/14-PATTERNS.md
@dashboard/lib/telegram-widget.server.test.ts
@dashboard/tests/e2e/dashboard-token.spec.ts
@dashboard/playwright.config.ts

<interfaces>
<!-- Locked contracts — executor copies these verbatim -->

Header names (lowercased in Headers API):
- `x-voidnet-user-id`     (i64, regex `^-?\d+$`)
- `x-voidnet-handle`      (3–32 chars, regex `^[a-z][a-z0-9-]+$`)
- `x-voidnet-telegram-id` (i64, must equal `OWNER_TELEGRAM_ID`)
- `x-voidnet-timestamp`   (unix seconds, regex `^\d+$`)
- `x-voidnet-signature`   (hex HMAC-SHA256, regex `^[0-9a-f]{64}$`)

Signature canonical message (FIXED — field order + `|` separator immutable):
  `${userId}|${handle}|${telegramId}|${timestamp}`
  then hmac_sha256(VOIDNET_HMAC_SECRET, msg) → hex

Replay window: ±60 seconds (Math.abs(now - ts) > 60 → reject)

Error codes (stable — E2E asserts on these strings):
- VOIDNET_SCHEMA           (status 401) — malformed header shape
- VOIDNET_STALE            (status 401) — timestamp out of ±60s window
- VOIDNET_SIG_INVALID      (status 401) — HMAC mismatch
- VOIDNET_OWNER_MISMATCH   (status 403) — telegramId ≠ OWNER_TELEGRAM_ID

Error body shape: `{ "error": "<human string>", "code": "<VOIDNET_*>" }`

NextAuth JWT salt (must match middleware getToken cookieName at middleware.ts:132-134):
- prod:  "__Secure-authjs.session-token"
- dev:   "authjs.session-token"

Synthetic JWT token fields (shape-identical to Telegram path):
  { sub: telegramId, telegramId: telegramId, name: handle, src: "voidnet" }
Session max-age: 60 * 60 * 8 (8 hours)

Test secret (for tests only): "test-voidnet-secret"
Test OWNER_TELEGRAM_ID (match existing Playwright fixture convention): check playwright.config.ts for current value; use same.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create unit test scaffold for verifyVoidnetHeaders</name>
  <files>dashboard/lib/voidnet-auth.server.test.ts</files>
  <read_first>
    - .planning/phases/14-voidnet-integration-auth/14-SPEC.md (acceptance criteria 1-6)
    - .planning/phases/14-voidnet-integration-auth/14-PATTERNS.md (unit test analog section)
    - dashboard/lib/telegram-widget.server.test.ts (structural analog)
  </read_first>
  <behavior>
    Tests exist for each case listed below; all currently RED (verifier not implemented yet):
    - "accepts valid headers (claims match, ok:true)"
    - "rejects tampered signature → code VOIDNET_SIG_INVALID, status 401"
    - "rejects bad handle too short (2 chars) → VOIDNET_SCHEMA"
    - "rejects bad handle bad chars (uppercase) → VOIDNET_SCHEMA"
    - "rejects non-numeric user_id → VOIDNET_SCHEMA"
    - "rejects non-numeric telegram_id → VOIDNET_SCHEMA"
    - "rejects non-numeric timestamp → VOIDNET_SCHEMA"
    - "rejects signature not 64-hex → VOIDNET_SCHEMA"
    - "rejects timestamp 61s in past → VOIDNET_STALE"
    - "rejects timestamp 61s in future → VOIDNET_STALE"
    - "accepts timestamp -59s / +59s → ok:true"
    - "rejects owner mismatch (telegramId ≠ ownerTelegramId arg) → VOIDNET_OWNER_MISMATCH, status 403"
    - "cross-verified vector: message `42|testuser|111111|1700000000` with secret `test-voidnet-secret` → HMAC hex = d4b... (hand-compute with node:crypto at test time; lock canonical form)"
  </behavior>
  <action>
    Create `dashboard/lib/voidnet-auth.server.test.ts` mirroring `dashboard/lib/telegram-widget.server.test.ts` structure.

    Imports:
      import { describe, test, expect } from "bun:test";
      import crypto from "node:crypto";
      import { verifyVoidnetHeaders } from "./voidnet-auth.server";

    (import target does not exist yet — tests will RED with module-not-found, which is the expected Wave-0 signal per RESEARCH.md Wave 0 Gaps.)

    Define const SECRET = "test-voidnet-secret" and OWNER = "111111" at top.

    Helper `signHeaders({userId, handle, telegramId, timestamp})`:
      const msg = `${userId}|${handle}|${telegramId}|${timestamp}`;
      const hash = crypto.createHmac("sha256", SECRET).update(msg).digest("hex");
      return Headers object with five x-voidnet-* keys. Use `new Headers({...})`.

    Every test calls `await verifyVoidnetHeaders(headers, SECRET, OWNER)` and asserts on `result.ok`, `result.code`, `result.status`, or `result.claims` fields.

    CRITICAL for cross-verified vector test: compute the expected hex with node:crypto inline and assert `verifyVoidnetHeaders` returns ok:true with exactly the fixed input `userId=42, handle=testuser, telegramId=OWNER, timestamp=Math.floor(Date.now()/1000)`. This locks the canonical message format.

    For stale tests, pass explicit timestamps: `Math.floor(Date.now()/1000) - 61` and `+ 61`.
    For ±59s tests, pass `-59` and `+59`.
    For owner mismatch, sign with `telegramId: "999999"` and verify with OWNER="111111".
    For tampered sig, flip last hex char of signature.
  </action>
  <verify>
    <automated>cd dashboard && bun test lib/voidnet-auth.server.test.ts 2>&1 | grep -E "(FAIL|Cannot find module|voidnet-auth.server)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f dashboard/lib/voidnet-auth.server.test.ts`
    - `grep -c "VOIDNET_SCHEMA\|VOIDNET_STALE\|VOIDNET_SIG_INVALID\|VOIDNET_OWNER_MISMATCH" dashboard/lib/voidnet-auth.server.test.ts` ≥ 4
    - `grep -q 'verifyVoidnetHeaders' dashboard/lib/voidnet-auth.server.test.ts`
    - `grep -q 'userId}|\${handle}|\${telegramId}|\${timestamp\|userId}|${handle}|${telegramId}|${timestamp' dashboard/lib/voidnet-auth.server.test.ts` (canonical string present)
    - `bun test` exits non-zero because module is missing (expected RED)
  </acceptance_criteria>
  <done>Unit test file committed, runs, fails with "Cannot find module ./voidnet-auth.server" or equivalent — RED state confirmed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create JWT mint-and-decode roundtrip smoke test</name>
  <files>dashboard/lib/voidnet-jwt-roundtrip.test.ts</files>
  <read_first>
    - .planning/phases/14-voidnet-integration-auth/14-RESEARCH.md (Pattern 2 + Pitfall 1 salt mismatch)
    - dashboard/middleware.ts (lines 127-135 — cookieName conditional)
  </read_first>
  <behavior>
    - Test "encode with prod salt → decode with prod salt yields same telegramId+sub+name" passes GREEN immediately (pure SDK roundtrip, no project code needed).
    - Test "encode with mismatched salt → decode returns null" passes GREEN, proving Pitfall 1 is covered.
    - Test acts as A1/A3 assumption check from RESEARCH.md so Wave 2 middleware work can trust encode() interop.
  </behavior>
  <action>
    Create `dashboard/lib/voidnet-jwt-roundtrip.test.ts`.

    Imports:
      import { describe, test, expect } from "bun:test";
      import { encode, decode } from "next-auth/jwt";

    Two tests:

    Test A — roundtrip:
      const SECRET = "a".repeat(64); // matches typical AUTH_SECRET length
      const SALT = "__Secure-authjs.session-token";
      const jwt = await encode({
        token: { sub: "111111", telegramId: "111111", name: "testuser", src: "voidnet" },
        secret: SECRET,
        maxAge: 60 * 60 * 8,
        salt: SALT,
      });
      const decoded = await decode({ token: jwt, secret: SECRET, salt: SALT });
      expect(decoded?.sub).toBe("111111");
      expect((decoded as any)?.telegramId).toBe("111111");
      expect((decoded as any)?.name).toBe("testuser");

    Test B — salt mismatch yields null:
      const jwt = await encode({ token: {...}, secret: SECRET, maxAge: 28800, salt: "authjs.session-token" });
      const decoded = await decode({ token: jwt, secret: SECRET, salt: "__Secure-authjs.session-token" });
      expect(decoded).toBeNull();

    Purpose documented in file header comment: "Locks A1/A3 assumption from 14-RESEARCH.md — next-auth/jwt encode()/decode() with matching salt is the Auth.js v5 path getToken() uses. If this test fails, Plan 02 verifier strategy must fall back to jose directly."
  </action>
  <verify>
    <automated>cd dashboard && bun test lib/voidnet-jwt-roundtrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f dashboard/lib/voidnet-jwt-roundtrip.test.ts`
    - `grep -q 'next-auth/jwt' dashboard/lib/voidnet-jwt-roundtrip.test.ts`
    - `grep -q '__Secure-authjs.session-token' dashboard/lib/voidnet-jwt-roundtrip.test.ts`
    - `bun test lib/voidnet-jwt-roundtrip.test.ts` exits 0 (GREEN — SDK available, interop works)
  </acceptance_criteria>
  <done>SDK smoke passes; Plan 02 can rely on `next-auth/jwt` encode path. If this test fails, halt and raise to planner — fallback to direct `jose` needed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create E2E helper + spec + inject env into playwright.config</name>
  <files>dashboard/tests/e2e/_voidnet-helpers.ts, dashboard/tests/e2e/voidnet.spec.ts, dashboard/playwright.config.ts</files>
  <read_first>
    - dashboard/tests/e2e/dashboard-token.spec.ts (header-injection analog)
    - dashboard/tests/e2e/auth.spec.ts (no-headers Telegram fallback analog)
    - dashboard/playwright.config.ts (webServer.env existing shape)
    - .planning/phases/14-voidnet-integration-auth/14-PATTERNS.md (E2E section)
  </read_first>
  <behavior>
    - `voidnetHeaders({telegramId?, tamper?, offset?})` returns Record with all five x-voidnet-* headers, HMAC computed via node:crypto with SECRET="test-voidnet-secret".
    - Seven E2E tests exist (initially RED — middleware voidnet path not implemented until Plan 03):
      1. "valid voidnet headers grant access without Telegram widget redirect"
      2. "tampered signature → 401 JSON {error, code: VOIDNET_SIG_INVALID}"
      3. "stale timestamp (-90s) → 401 JSON {error, code: VOIDNET_STALE}"
      4. "owner mismatch (telegramId=999999) → 403 JSON {error, code: VOIDNET_OWNER_MISMATCH}"
      5. "no voidnet headers → Telegram flow unchanged (redirect to /login)"
      6. "GET /api/integration/v1/meta with valid sig → 200 {version, supported_auth_modes:['telegram','voidnet'], dashboard_port:8090}"
      7. "GET /api/integration/v1/meta without sig → 401"
    - playwright.config.ts webServer env includes `VOIDNET_HMAC_SECRET=test-voidnet-secret` and keeps existing `OWNER_TELEGRAM_ID` (verify existing value and mirror it in _voidnet-helpers.ts as OWNER constant).
  </behavior>
  <action>
    1. Create `dashboard/tests/e2e/_voidnet-helpers.ts`:

       import crypto from "node:crypto";

       export const SECRET = "test-voidnet-secret";
       // Must match OWNER_TELEGRAM_ID in playwright.config.ts webServer.env.
       // Read playwright.config.ts; if existing value is e.g. "111111", use that.
       export const OWNER = "<match-playwright-config-value>";

       export function voidnetHeaders(opts: {telegramId?: string; tamper?: boolean; offset?: number} = {}): Record<string,string> {
         const telegramId = opts.telegramId ?? OWNER;
         const userId = "42";
         const handle = "testuser";
         const ts = Math.floor(Date.now()/1000) + (opts.offset ?? 0);
         const msg = `${userId}|${handle}|${telegramId}|${ts}`;
         let sig = crypto.createHmac("sha256", SECRET).update(msg).digest("hex");
         if (opts.tamper) sig = sig.slice(0,-1) + (sig.slice(-1) === "0" ? "1" : "0");
         return {
           "x-voidnet-user-id": userId,
           "x-voidnet-handle": handle,
           "x-voidnet-telegram-id": telegramId,
           "x-voidnet-timestamp": String(ts),
           "x-voidnet-signature": sig,
         };
       }

    2. Create `dashboard/tests/e2e/voidnet.spec.ts` mirroring dashboard-token.spec.ts. Seven tests as listed in <behavior>. For each error test use `page.request.get("/", {headers: voidnetHeaders({...})})` then assert `res.status()` and `await res.json()` contains `{error: <string>, code: <VOIDNET_*>}`. For "valid" test, open a page with `extraHTTPHeaders: voidnetHeaders()` in browser.newContext and assert URL does not redirect to /login. For "no headers" test, mirror auth.spec.ts redirect assertion.

    3. Edit `dashboard/playwright.config.ts`: add `VOIDNET_HMAC_SECRET: "test-voidnet-secret"` to the `webServer.env` object (find the existing env block — it already sets DASHBOARD_TOKEN/OWNER_TELEGRAM_ID). If OWNER_TELEGRAM_ID is not currently set there, ADD `OWNER_TELEGRAM_ID: "111111"` too and use that same value in _voidnet-helpers.ts.

    Do NOT start the verifier implementation. These tests will be RED until Plan 03 wires middleware.
  </action>
  <verify>
    <automated>cd dashboard && ls tests/e2e/voidnet.spec.ts tests/e2e/_voidnet-helpers.ts && grep -q 'VOIDNET_HMAC_SECRET' playwright.config.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f dashboard/tests/e2e/voidnet.spec.ts`
    - `test -f dashboard/tests/e2e/_voidnet-helpers.ts`
    - `grep -q 'VOIDNET_HMAC_SECRET' dashboard/playwright.config.ts`
    - `grep -q 'OWNER_TELEGRAM_ID' dashboard/playwright.config.ts`
    - `grep -c 'VOIDNET_SIG_INVALID\|VOIDNET_STALE\|VOIDNET_OWNER_MISMATCH' dashboard/tests/e2e/voidnet.spec.ts` ≥ 3
    - `grep -q '/api/integration/v1/meta' dashboard/tests/e2e/voidnet.spec.ts`
    - OWNER constant in _voidnet-helpers.ts matches OWNER_TELEGRAM_ID in playwright.config.ts (manually verified by grep diff)
  </acceptance_criteria>
  <done>E2E spec + helper + config committed. Running `bunx playwright test voidnet` will RED (expected — middleware voidnet path ships in Plan 03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → verifier module | Tests invoke verifier with attacker-shaped inputs to assert rejection |
| playwright webServer env → dashboard process | Test-only secret enters via config; never committed as real secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering (replay) | voidnet verifier timestamp path | mitigate | Unit tests assert reject at ±61s, accept at ±59s (Task 1 STALE tests lock ±60s window) |
| T-14-02 | Spoofing (signature forgery) | voidnet verifier HMAC compare | mitigate | Cross-verified hex vector test in Task 1 locks canonical `{userId}|{handle}|{telegramId}|{timestamp}` message format; tampered-sig test asserts VOIDNET_SIG_INVALID |
| T-14-03 | Elevation of Privilege (owner bypass) | verifier owner check | mitigate | Task 1 owner-mismatch test asserts 403 + VOIDNET_OWNER_MISMATCH with sig valid but telegramId ≠ OWNER |
| T-14-04 | Information Disclosure (error leak) | error body shape | mitigate | Tasks 1 + 3 only assert on fixed strings `VOIDNET_*` — never on header echoes or internal HMAC values. If Plan 02/03 leaks, assertions will not match and CI fails closed |
</threat_model>

<verification>
- `cd dashboard && bun test lib/voidnet-jwt-roundtrip.test.ts` GREEN (SDK interop confirmed)
- `cd dashboard && bun test lib/voidnet-auth.server.test.ts` RED with Cannot-find-module or similar (expected pre-Plan-02)
- `cd dashboard && grep -q VOIDNET_HMAC_SECRET playwright.config.ts`
- Files exist: both test files, helper, spec
</verification>

<success_criteria>
All five files exist and committed. JWT roundtrip smoke passes. Verifier unit spec is RED in the expected way (module missing). E2E spec is created but not yet runnable to GREEN. playwright.config.ts carries VOIDNET_HMAC_SECRET so later waves can exercise the full middleware.
</success_criteria>

<output>
Create `.planning/phases/14-voidnet-integration-auth/14-01-SUMMARY.md` with: files created, JWT smoke result, which unit test names are RED, any deviations from OWNER value chosen.
</output>
