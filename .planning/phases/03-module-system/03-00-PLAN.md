---
phase: 03-module-system
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - tests/modules/__init__.py
  - tests/modules/conftest.py
  - tests/modules/test_manifest.py
  - tests/modules/test_registry.py
  - tests/modules/test_lifecycle.py
  - tests/modules/test_assembler.py
  - tests/modules/test_roundtrip.py
  - tests/modules/test_isolation.py
  - tests/modules/fixtures/valid-module/manifest.json
  - tests/modules/fixtures/valid-module/install.sh
  - tests/modules/fixtures/valid-module/uninstall.sh
  - tests/modules/fixtures/valid-module/prompt.md
  - tests/modules/fixtures/invalid-manifest/manifest.json
autonomous: true
requirements: []
nyquist_compliant: true

must_haves:
  truths:
    - "pydantic v2 is importable in the project venv"
    - "tests/modules/ test package exists and is discovered by pytest"
    - "Test stubs exist for manifest, registry, lifecycle, assembler, roundtrip, isolation"
    - "Valid and invalid manifest fixtures exist for strict validation testing"
  artifacts:
    - path: "pyproject.toml"
      provides: "pydantic>=2.0 declared as direct dependency"
      contains: "pydantic>=2.0"
    - path: "tests/modules/conftest.py"
      provides: "Shared fixtures: tmp_hub_dir, sample_manifest, bridge_module_dir"
      min_lines: 30
    - path: "tests/modules/fixtures/valid-module/manifest.json"
      provides: "Reference valid manifest for fixture-based tests"
    - path: "tests/modules/fixtures/invalid-manifest/manifest.json"
      provides: "Malformed manifest (unknown field) for strict rejection tests"
  key_links:
    - from: "pyproject.toml"
      to: "bot/modules/manifest.py (future)"
      via: "pydantic>=2.0 dependency"
      pattern: "pydantic>=2\\.0"
    - from: "tests/modules/conftest.py"
      to: "tests/modules/fixtures/valid-module/"
      via: "bridge_module_dir / valid_module_dir fixture yields Path"
      pattern: "fixtures/valid-module"
---

<objective>
Wave 0 infrastructure for Phase 3. Adds pydantic as a direct dependency, creates tests/modules/ package with shared fixtures and failing-test stubs, and provides valid + invalid manifest fixtures for subsequent wave tests.

Purpose: All Phase 3 implementation plans verify via `python -m pytest tests/modules/...`. Those tests cannot be written before the test package and fixtures exist. This plan unblocks Waves 1-3.

Output: pyproject.toml with pydantic dep, tests/modules/ package scaffold, six test stub files (all initially skip/xfail until implementation lands), two fixture module directories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-module-system/03-CONTEXT.md
@.planning/phases/03-module-system/03-RESEARCH.md
@.planning/phases/03-module-system/03-VALIDATION.md
@pyproject.toml
@tests/conftest.py
@tests/test_install.py

<interfaces>
<!-- Existing pytest configuration (from pyproject.toml) -->
[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

<!-- Existing dependencies (pyproject.toml [project].dependencies) -->
python-telegram-bot>=21.10
claude-code-sdk>=0.0.25
httpx[socks]>=0.27.0
fastapi>=0.115.0
uvicorn>=0.30.0
jinja2>=3.1.0
itsdangerous>=2.1.0

<!-- Shared fixture surface this plan creates (consumed by all Wave 1-3 tests) -->
@pytest.fixture
def tmp_hub_dir(tmp_path: Path) -> Path:
    """Temp Hub directory mimicking ~/hub/knowledge/animaya/ layout."""

@pytest.fixture
def valid_module_dir(tmp_path: Path) -> Path:
    """Copy of tests/modules/fixtures/valid-module/ into tmp_path."""

@pytest.fixture
def invalid_manifest_dir(tmp_path: Path) -> Path:
    """Copy of tests/modules/fixtures/invalid-manifest/ into tmp_path."""

@pytest.fixture
def sample_manifest_dict() -> dict:
    """Plain dict matching ModuleManifest v1 — for unit tests without touching disk."""
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pydantic>=2.0 to pyproject.toml and install</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (to see current [project].dependencies list)
    - .planning/phases/03-module-system/03-RESEARCH.md (§Environment Availability confirms pydantic absent; §Pitfall 1 explains why this must be Wave 0)
    - .planning/phases/03-module-system/03-CONTEXT.md (D-08, D-10 motivate strict pydantic use)
  </read_first>
  <action>
    Edit `pyproject.toml` to append `"pydantic>=2.0",` as the last entry in `[project].dependencies` (after `"itsdangerous>=2.1.0"`). Preserve existing line ordering and trailing-comma convention. Do NOT touch `[project.optional-dependencies]`, `[tool.ruff]`, or `[tool.pytest.ini_options]`.

    After editing, reinstall the editable package so pydantic is importable: run `pip install -e ".[dev]" --quiet`.

    Verify import works via `python -c "import pydantic; assert pydantic.VERSION.startswith('2.'), pydantic.VERSION; print('pydantic', pydantic.VERSION)"`.

    Rationale: per D-10 (strict validation) and RESEARCH §Pitfall 1, pydantic is transitively pulled by FastAPI but NOT declared — a fresh venv without FastAPI's deps would break. Declaring it directly is the correct fix.
  </action>
  <verify>
    <automated>python -c "import pydantic; assert pydantic.VERSION.startswith('2.')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '^\s*"pydantic>=2\.0"' pyproject.toml` returns exactly one match inside `[project].dependencies`
    - `python -c "import pydantic; print(pydantic.VERSION)"` prints a version starting with `2.`
    - `python -c "from pydantic import BaseModel, ConfigDict; ConfigDict(extra='forbid')"` runs without error (confirms strict-mode API available)
    - No changes outside `[project].dependencies` list (diff against HEAD shows only one-line addition)
  </acceptance_criteria>
  <done>pydantic>=2.0 declared and importable; `pydantic.VERSION` starts with "2."</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/modules/ package, fixtures module, and fixture module directories</name>
  <files>
    tests/modules/__init__.py
    tests/modules/conftest.py
    tests/modules/fixtures/valid-module/manifest.json
    tests/modules/fixtures/valid-module/install.sh
    tests/modules/fixtures/valid-module/uninstall.sh
    tests/modules/fixtures/valid-module/prompt.md
    tests/modules/fixtures/invalid-manifest/manifest.json
  </files>
  <read_first>
    - tests/conftest.py (existing fixture style — reuse patterns)
    - tests/test_install.py (existing tmp_path / monkeypatch patterns)
    - .planning/phases/03-module-system/03-VALIDATION.md (§Wave 0 Requirements lists exactly these artifacts)
    - .planning/phases/03-module-system/03-RESEARCH.md (§Code Examples — Manifest JSON for bridge module; use as template for the valid fixture)
  </read_first>
  <action>
    Create the following files exactly.

    1. `tests/modules/__init__.py` — empty file (package marker).

    2. `tests/modules/conftest.py` — define four fixtures. Must have module docstring. Imports: `import shutil, pytest`, `from pathlib import Path`.

       Fixtures (all session-independent, function-scoped):
       - `tmp_hub_dir(tmp_path)` — returns `tmp_path / "hub" / "knowledge" / "animaya"`; calls `.mkdir(parents=True, exist_ok=True)`.
       - `valid_module_dir(tmp_path)` — copies `tests/modules/fixtures/valid-module/` to `tmp_path / "modules" / "sample"` using `shutil.copytree`. Ensures install.sh and uninstall.sh are chmod 0o755. Returns the destination Path.
       - `invalid_manifest_dir(tmp_path)` — copies `tests/modules/fixtures/invalid-manifest/` to `tmp_path / "modules" / "bad"`. Returns destination Path.
       - `sample_manifest_dict()` — returns a plain dict: `{"manifest_version": 1, "name": "sample", "version": "1.0.0", "system_prompt_path": "prompt.md", "owned_paths": [], "scripts": {"install": "install.sh", "uninstall": "uninstall.sh"}, "depends": [], "config_schema": None}`. (This matches the ModuleManifest schema Plan 01 will build.)

       Use type hints throughout (`def tmp_hub_dir(tmp_path: Path) -> Path:`).

    3. `tests/modules/fixtures/valid-module/manifest.json` — strict-schema-valid manifest:
       ```json
       {
         "manifest_version": 1,
         "name": "sample",
         "version": "1.0.0",
         "system_prompt_path": "prompt.md",
         "owned_paths": [],
         "scripts": {"install": "install.sh", "uninstall": "uninstall.sh"},
         "depends": [],
         "config_schema": null
       }
       ```

    4. `tests/modules/fixtures/valid-module/install.sh` — `#!/usr/bin/env bash` with `set -euo pipefail`. Echoes `"sample install: ${ANIMAYA_MODULE_DIR}"` to stdout. Touches `${ANIMAYA_HUB_DIR}/.sample-marker` if `ANIMAYA_HUB_DIR` is set (used by lifecycle tests to verify env injection). Exits 0. Must be executable (chmod 0o755).

    5. `tests/modules/fixtures/valid-module/uninstall.sh` — `#!/usr/bin/env bash` with `set -euo pipefail`. Echoes `"sample uninstall"`. Removes `${ANIMAYA_HUB_DIR}/.sample-marker` if present: `rm -f "${ANIMAYA_HUB_DIR}/.sample-marker"` (idempotent per RESEARCH Pitfall 3). Exits 0. Must be executable (chmod 0o755).

    6. `tests/modules/fixtures/valid-module/prompt.md` — single paragraph: `"You are the sample module. Use this prompt in tests."`.

    7. `tests/modules/fixtures/invalid-manifest/manifest.json` — contains an unknown field `"unexpected_field"` to trigger pydantic strict rejection:
       ```json
       {
         "manifest_version": 1,
         "name": "bad",
         "version": "1.0.0",
         "system_prompt_path": "prompt.md",
         "owned_paths": [],
         "unexpected_field": "should cause ValidationError"
       }
       ```

    Use `Path.write_text()` / `Path.chmod()` — never string concatenation for paths. For shell scripts, include trailing newline.
  </action>
  <verify>
    <automated>python -m pytest tests/modules/ --collect-only -q 2>&1 | grep -E "fixtures|0 errors|conftest" || echo "collection OK"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/modules/__init__.py` exists and is empty (0 bytes or just a newline)
    - `grep -c '^def ' tests/modules/conftest.py` returns `>= 4` (four fixtures)
    - `grep -E '@pytest\.fixture' tests/modules/conftest.py` has at least 4 matches
    - `tests/modules/fixtures/valid-module/manifest.json` parses as JSON: `python -c "import json; json.load(open('tests/modules/fixtures/valid-module/manifest.json'))"` exits 0
    - `tests/modules/fixtures/invalid-manifest/manifest.json` contains the literal string `unexpected_field`
    - `[ -x tests/modules/fixtures/valid-module/install.sh ]` returns 0 (file is executable)
    - `[ -x tests/modules/fixtures/valid-module/uninstall.sh ]` returns 0
    - `head -1 tests/modules/fixtures/valid-module/install.sh` equals `#!/usr/bin/env bash`
    - `grep -q 'set -euo pipefail' tests/modules/fixtures/valid-module/install.sh` exits 0
    - `python -m pytest tests/modules/ --collect-only -q` prints no errors (may show 0 tests until Task 3 adds stubs)
  </acceptance_criteria>
  <done>Test package importable; fixtures define tmp_hub_dir, valid_module_dir, invalid_manifest_dir, sample_manifest_dict; valid + invalid module fixture dirs on disk; shell scripts executable with proper shebang.</done>
</task>

<task type="auto">
  <name>Task 3: Create six test stub files (all tests initially xfail/skip; Wave 1-3 implements)</name>
  <files>
    tests/modules/test_manifest.py
    tests/modules/test_registry.py
    tests/modules/test_lifecycle.py
    tests/modules/test_assembler.py
    tests/modules/test_roundtrip.py
    tests/modules/test_isolation.py
  </files>
  <read_first>
    - .planning/phases/03-module-system/03-VALIDATION.md (§Per-Task Verification Map — each stub matches a row)
    - tests/modules/conftest.py (fixtures available — just created)
    - tests/test_bridge.py (class-based style reference)
  </read_first>
  <action>
    Create six pytest stub files. Each file has module docstring, `import pytest`, and uses `pytest.importorskip("bot.modules", reason="Wave N not yet implemented")` at module top so tests are SKIPPED (not failed) until the relevant wave lands. Tests should be written with real assertions so that once `bot.modules` imports successfully, they actively validate behavior.

    Each file structure:
    ```python
    """Tests for <component> (Phase 3, MODS-XX)."""
    from __future__ import annotations

    import pytest

    # Skip entire module until bot.modules package exists (Wave 1+)
    pytest.importorskip("bot.modules", reason="bot.modules package not yet implemented")
    ```

    1. `tests/modules/test_manifest.py` — class `TestManifest`:
       - `test_valid_manifest_parses(valid_module_dir)` — loads manifest.json, calls `bot.modules.validate_manifest(valid_module_dir)`, asserts returned object has `.name == "sample"`, `.version == "1.0.0"`, `.manifest_version == 1`.
       - `test_invalid_rejected(invalid_manifest_dir)` — calls `bot.modules.validate_manifest(invalid_manifest_dir)` inside `pytest.raises(Exception)` (narrower ValidationError check once pydantic imported); asserts error message mentions `"unexpected_field"` or `"extra"`.
       - `test_missing_required_field_rejected(tmp_path, sample_manifest_dict)` — writes a manifest missing `name`, asserts validation raises.
       - `test_semver_prefix_accepted(sample_manifest_dict)` — direct dict validation via `ModuleManifest.model_validate(sample_manifest_dict)` succeeds.

    2. `tests/modules/test_registry.py` — class `TestRegistry`:
       - `test_read_missing_registry_returns_empty(tmp_hub_dir)` — `read_registry(tmp_hub_dir)` returns `{"modules": []}` when file absent.
       - `test_write_then_read_roundtrip(tmp_hub_dir)` — write entry, read, assert list contains entry.
       - `test_atomic_write_no_partial_file(tmp_hub_dir)` — after `write_registry`, no `.tmp` file remains in hub dir.
       - `test_list_installed_returns_names(tmp_hub_dir)` — write two entries, `list_installed(tmp_hub_dir)` returns their names in install order.

    3. `tests/modules/test_lifecycle.py` — class `TestLifecycle`:
       - `test_install_runs_script_and_updates_registry(valid_module_dir, tmp_hub_dir, monkeypatch)` — invoke install API; assert `.sample-marker` created in hub_dir, registry has entry.
       - `test_install_rejects_already_installed(valid_module_dir, tmp_hub_dir)` — install twice; second raises with message containing `"already installed"`.
       - `test_install_failure_triggers_rollback(tmp_path, tmp_hub_dir)` — create module with failing install.sh (exit 1); install call raises; registry stays empty; owned_paths absent.
       - `test_uninstall_removes_registry_entry(valid_module_dir, tmp_hub_dir)` — install then uninstall; registry empty; `.sample-marker` gone.
       - `test_uninstall_of_uninstalled_module_rejected(tmp_hub_dir)` — raises with message mentioning `"not installed"`.
       - `test_missing_dependency_rejected(tmp_path, tmp_hub_dir)` — module with `depends: ["missing"]` raises with `"missing dependency"`.

    4. `tests/modules/test_assembler.py` — class `TestAssembler`:
       - `test_assembler_writes_base_only_when_empty_registry(tmp_hub_dir, monkeypatch)` — no modules installed; output CLAUDE.md contains base template AND marker `<!-- No modules installed -->`.
       - `test_assembler_merges_installed_module_prompt(valid_module_dir, tmp_hub_dir, monkeypatch)` — install sample; output contains `<module name="sample">` and the prompt text.
       - `test_assembler_preserves_install_order(tmp_path, tmp_hub_dir)` — two modules with ascending installed_at; assembler output has first module's `<module name=...>` block appearing before the second.

    5. `tests/modules/test_roundtrip.py` — class `TestRoundtrip`:
       - `test_install_uninstall_leaves_no_artifacts(valid_module_dir, tmp_hub_dir)` — install, assert marker exists; uninstall, assert marker absent AND no files remain under hub_dir that are declared in owned_paths.
       - `test_registry_is_empty_after_roundtrip(valid_module_dir, tmp_hub_dir)` — after full cycle, `list_installed(tmp_hub_dir) == []`.

    6. `tests/modules/test_isolation.py` — class `TestIsolation`:
       - `test_no_cross_module_imports()` — AST-walk every `.py` file under `modules/*/` (if any). For each Import/ImportFrom node, assert module name does not start with `modules.` or reference another sibling module directory. In Phase 3 only the bridge module exists (and it has no Python in `modules/bridge/`, only shell + markdown), so the test must handle empty case gracefully and STILL EXIST to enforce the rule going forward.
       - `test_no_imports_between_modules_registered_in_registry(tmp_hub_dir)` — similar but iterates registry entries.

    For each test file, ensure the `importorskip` at top means: when `bot.modules` does not exist yet, the whole module is skipped cleanly; when it does, all tests run.

    No actual implementation of `bot.modules.*` in this task — just stubs and real assertions that will activate as each wave implements.
  </action>
  <verify>
    <automated>python -m pytest tests/modules/ -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All six files exist and are syntactically valid Python: `python -c "import ast; [ast.parse(open(f).read()) for f in ['tests/modules/test_manifest.py','tests/modules/test_registry.py','tests/modules/test_lifecycle.py','tests/modules/test_assembler.py','tests/modules/test_roundtrip.py','tests/modules/test_isolation.py']]"` exits 0
    - Each file has `pytest.importorskip("bot.modules"` near the top (grep match)
    - `grep -E '^class Test' tests/modules/test_manifest.py` finds `TestManifest`; same for the other five class names
    - `python -m pytest tests/modules/ -v` runs and either skips all (expected now — bot.modules missing) or shows collected counts; NO collection errors
    - `python -m pytest tests/modules/ -q --collect-only` reports at least 17 tests total (4 manifest + 4 registry + 6 lifecycle + 3 assembler + 2 roundtrip + 2 isolation = 21; skipping is OK but collection must succeed)
  </acceptance_criteria>
  <done>Six test stubs present. Tests are skipped cleanly (via importorskip) until bot.modules lands. Collection succeeds with no errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None (Wave 0 is infra) | This plan only touches dev dependencies and test scaffolding. No runtime code, no user input, no external service boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-00-01 | Tampering | pyproject.toml dependency list | accept | Direct edit only adds `pydantic>=2.0`; pydantic is a well-known, widely-audited library already transitively present. No supply-chain change. |
| T-03-00-02 | Information Disclosure | tests/modules/fixtures/ | accept | Fixtures contain only synthetic sample data (no secrets, no credentials). Checked into repo, visible in git history — intentional. |
| T-03-00-03 | Denial of Service | test install.sh creating files in tmp_hub_dir | mitigate | install.sh uses `${ANIMAYA_HUB_DIR}` (pytest tmp_path only during tests). No write outside tmp. `set -euo pipefail` prevents silent-path-missing bugs. |
</threat_model>

<verification>
Wave 0 is complete when:
1. `python -c "import pydantic; assert pydantic.VERSION.startswith('2.')"` succeeds.
2. `python -m pytest tests/modules/ -v` collects ≥17 tests without errors (all skipped expected until Wave 1).
3. Both fixture module directories contain all declared files with correct permissions.
</verification>

<success_criteria>
- pydantic>=2.0 installed and importable
- tests/modules/ package present with conftest + 6 test stubs + 2 fixture module dirs
- All six subsequent waves can now add implementation and have tests switch from skip → pass
- No changes to bot/ source tree (infra only)
</success_criteria>

<output>
After completion, create `.planning/phases/03-module-system/03-00-SUMMARY.md`.
</output>
