# Milestones

## v2.0 Onboarding Polish & Bridge-as-Module (Shipped: 2026-04-17)

**Phases completed:** 3 phases, 12 plans, 25 tasks

**Key accomplishments:**

- One-liner:
- One-liner:
- One-liner:
- One-liner:
- 1. [Rule 3 - Blocking] `bunx tsc --noEmit` failed on `import "./globals.css"` side-effect (TS2882)
- 1. [Rule 1 — Bug] Telegram-token regex missed fixtures with suffix length > 35
- 1. [Rule 1 — Bug] DENY set omitted top-level `.git`
- 1. [Rule 1 — Bug] `stream_chat` heartbeat used `asyncio.wait_for(shield(task))` which re-entered the async iterator
- 1. [Rule 3 — Blocking] Created `lib/schemas.ts` locally
- DELETED (HTTP-route tests — covered by Playwright):

---
