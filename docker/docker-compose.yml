services:
  bot:
    build:
      context: ..
      dockerfile: docker/Dockerfile.bot
    container_name: animaya-bot
    restart: unless-stopped
    env_file: ../.env
    volumes:
      - bot-data:/data
    ports:
      - "8090:8090"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    # Resource limits (sandbox hardening deferred to Phase 3)
    mem_limit: 2g
    cpus: 1.0
    pids_limit: 256

volumes:
  bot-data:
    driver: local
